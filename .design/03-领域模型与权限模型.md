# 领域模型与权限模型

本文是平台实体、关系、状态与权限表达的唯一来源。它描述逻辑数据，不指定物理表、数据库或代码目录。

## 1. 权威矩阵

| 事实 | 唯一权威 | 其他系统中的形态 |
|---|---|---|
| OIDC browser authentication session | AgentGateway | 加密 HttpOnly cookie；Core 不读取 ID token |
| Human identity、platform session | Platform Core | OIDC subject 投影 |
| Tenant/Workspace membership 与收敛状态 | Platform Core | SpiceDB tenant/workspace relationships 与 Buzz relay/Channel roster 投影 |
| HUMAN 的 Workspace 收藏/静音与 Channel/Message/Thread 已读位置 | Platform Core | Browser 只有会话缓存；Relay kind:30078 不是一期权威 |
| Tenant、Workspace、Resource、Asset、owner | Platform Core | Buzz/组件 binding、SpiceDB relationships |
| 访问允许/拒绝 | SpiceDB | ActionDecision 中的 ZedToken 与 evidence |
| Approval/Workflow lifecycle | Temporal | Core TaskProjection 只读视图 |
| Buzz Community/Channel/message/thread/event | Buzz Relay | Core binding、AuditEvent 与 AgentInvocation 引用 |
| Agent Definition、Version、Installation、Skill 与有效配置投影 | Platform Core | Buzz Persona/identity、Codex config/skill/MCP、AgentGateway ConfigResource 投影 |
| Agent Installation 的 core/cold 长期记忆 head | Buzz Relay NIP-AE | AgentMemoryBinding 与 AgentSession 只保存 binding/event ref，不复制正文 |
| Agent session thread/turn/item | Codex ThreadStore/Rollout | AgentSession/AgentInvocation 固定 thread、version、projection generation 与 core event 快照 |
| Tool transport/route | AgentGateway | Core ToolDefinition 的运行投影 |
| Entitlement、credit、committed usage、billing | OpenMeter | Core QuotaReservation/UsageOutbox |
| 平台 runtime slot | Platform Core | CapacityLease |
| 组件业务正文与 native task | 对应应用组件 | Resource/ApplicationBinding/ExternalExecution |
| 文件字节、node UUID 与版本 | Cells | FileReference、DocumentSession 与 Buzz event 只存稳定引用 |
| 文档编辑协同运行状态 | DocumentServer | Core 只存 DocumentSession 的门禁、WOPI launch/save evidence 与最终 Cells revision |
| Platform AuditEvent | Platform Core | Buzz/Temporal/SpiceDB/OpenMeter/native IDs 作 evidence |

## 2. 身份、作用域与 Buzz 投影

```text
IdentityProvider(id, issuer, client_id, claim_mapping_version, status)
HumanIdentity(id, display_name, status)
ExternalIdentity(id, provider_id, issuer, subject, human_identity_id, status)

Tenant(id, slug, name,
       state[PROVISIONING|ACTIVE|SUSPENDING|SUSPENDED|RESTORING|DELETING|DELETED|ERROR],
       version)
Workspace(id, tenant_id, slug, name,
          state[PROVISIONING|ACTIVE|SUSPENDING|SUSPENDED|RESTORING|ERROR],
          version)
TenantMembership(id, tenant_id, human_identity_id,
                 tenant_principal_id,
                 state[INVITED|PROVISIONING|ACTIVE|REVOKING|REVOKED|ERROR], version)
WorkspaceMembership(id, workspace_id, tenant_principal_id,
                    state[PROVISIONING|ACTIVE|REVOKING|REVOKED|ERROR], version)

Principal(id, tenant_id, kind[HUMAN|AGENT|SERVICE], status)
AgentPrincipal(principal_id, agent_installation_resource_id)
ServicePrincipal(principal_id, audience, component_binding_ref[kind,id] | NONE)

PlatformSession(id, human_identity_id, tenant_membership_id,
                current_workspace_id | NONE, issued_at, expires_at, status)

CollaborationUserState(
  tenant_principal_id,
  workspace_preferences[workspace_id -> {starred, muted, updated_at}],
  read_contexts[context_key -> last_read_at],
  version
)

TenantBuzzBinding(tenant_id, community_id, normalized_host,
                  control_service_principal_id,
                  require_relay_membership, allow_nip_oa_auth,
                  pubkey_allowlist_enabled,
                  nip11_snapshot_digest, nip11_observed_at,
                  state[UNBOUND|PROVISIONING|RECONCILING|ACTIVE|DISABLED], version)
WorkspaceBuzzBinding(workspace_id, channel_id,
                     state[UNBOUND|PROVISIONING|RECONCILING|ACTIVE|DISABLED], version)
BuzzIdentityBinding(pubkey, tenant_id, principal_id,
                    custody[SERVER|CLIENT], private_key_secret_ref?,
                    kind[HUMAN|AGENT|CONTROL],
                    state[PENDING_SECRET|RECONCILING|ACTIVE|REVOKING|REVOKED], version)

RelayOperatorIdentity(catalog_tenant_id, pubkey,
                      private_key_secret_ref, audience,
                      relay_operator_api_origin,
                      state[PENDING_SECRET|ACTIVE|REVOKED], version)
```

不变式：

- `(provider_id, issuer, subject)` 只映射一个 HumanIdentity。
- HumanIdentity 在每个 Tenant 至多一个非 `REVOKED` TenantMembership/HUMAN Principal。TenantMembership `ACTIVE` 只在 Core membership、SpiceDB tenant relationship 与 Buzz `relay_members` 全部对账后成立；`INVITED/PROVISIONING/REVOKING/REVOKED/ERROR` 均不能建立 PlatformSession 或发起新动作（DD-45）。
- WorkspaceMembership 引用的 Workspace 与 HUMAN Principal 必须同 Tenant；同一 `(workspace_id, tenant_principal_id)` 至多一个 active membership。`ACTIVE` 只在 Core membership、SpiceDB relationship 与 Buzz Channel roster 全部对账后成立；`REVOKING/REVOKED/ERROR` 均拒绝新 scope（DD-41）。
- 一个 HumanIdentity 可在多个 Tenant 各有一条 `ACTIVE` TenantMembership，但一期不提供登录时选择 Tenant 的入口。此时身份解析以 `BLOCKED/TENANT_SELECTION_NOT_AVAILABLE` 拒绝并留审计，不按任何顺序任取其一——任取即回退默认租户。非 `ACTIVE` 的 membership 不参与选择，也不得遮住唯一 `ACTIVE` 的那一条。
- TenantMembership 进入 `REVOKING` 时立即撤销其 PlatformSession、关闭全部 Workspace stream 并拒绝新动作；HUMAN Principal 在收敛期间仅作为历史责任与 owner 转移标识，不能执行。除 Tenant 销毁外，其所有 active Resource/Asset 必须先由 Tenant admin 的受治理 owner-transfer Action 转给另一 active HUMAN，才能在 SpiceDB tenant relationship 与 Buzz relay roster 均已撤销后进入 `REVOKED`（DD-45）。
- Principal 只属于一个 Tenant；OIDC claim 只认证 HumanIdentity，不产生业务授权。
- TenantBuzzBinding 与 WorkspaceBuzzBinding 一对一；任一侧不一致时 scope 不可用。
- `TenantBuzzBinding` 与 `WorkspaceBuzzBinding` 的正常建立路径固定为 `UNBOUND → PROVISIONING → RECONCILING → ACTIVE`，停用进入 `DISABLED`；`BuzzIdentityBinding` 固定为 `PENDING_SECRET → RECONCILING → ACTIVE → REVOKING → REVOKED`。`RECONCILING` 必须核对 Relay roster；启用 `pubkey_allowlist` 时还必须写入并查证 allowlist 行。`ACTIVE` 的 Tenant binding 固定保存当前 NIP-11 文档的 canonical digest 与抓取时间，运行连接不得使用超出该快照声明值的订阅、filter、limit 或 frame 合同。
- 每个可在 Buzz 产生 event 的 HUMAN/AGENT 都有独立 BuzzIdentityBinding；不共享 pubkey。BuzzIdentityBinding 以 `pubkey` 为身份：一个 HUMAN Principal 在其 Tenant 内至多一条非 `REVOKED` 的 `custody=SERVER` binding（Buzz Web），另有每台原生设备一条 `custody=CLIENT` binding，设备数以部署登记的上界为限（DD-77）。一个人同时使用 Web 与原生端时，Relay 看到的是同一 Principal 名下的多个 pubkey；审计与作者显示按 pubkey 归因到该 Principal。
- 成员投影与撤权对该 Principal 的**全部**非 `REVOKED` binding 生效：建立时每个 pubkey 都进入 relay 与 Channel roster，撤权时全部移出，任一 pubkey 未收敛即整体未收敛（DD-77）。单台设备的 CLIENT binding 可单独撤销，只影响该 pubkey。
- `custody` 按 `09-统一身份与Buzz协议投影.md` 的托管表取值：Buzz Web 的 HUMAN、全部 AGENT 与 CONTROL 为 `SERVER`，`private_key_secret_ref` 必须非空；Buzz Desktop 与 Buzz Mobile 的 HUMAN 为 `CLIENT`，`private_key_secret_ref` 必须为空。`custody=CLIENT` 的 binding 跳过 `PENDING_SECRET`，自 `RECONCILING` 起始，其 `RECONCILING` 只核对 Relay roster 而不核对 secret 可读性。
- `custody` 不改变任何授权、审批、额度或审计判定；撤权对两种取值都以 SpiceDB relationship 与 Buzz roster 为执行点（`10` §4）。`custody=CLIENT` 时平台不具备停止该 pubkey 签名的手段，轮换语义相应收窄为 roster 移除加已知连接关闭。
- CONTROL 身份是 Buzz Community 的协议级 SERVICE owner，只作 Community/roster 投影与该 Tenant 的 NIP-AE Memory Counterparty/解密边界，不得成为 Resource/Asset owner 或业务 actor。
- `RelayOperatorIdentity` 是唯一部署级 Buzz operator signer，归 Platform Catalog Tenant 而不属于任一业务 Tenant。它只允许 audience `buzz-relay-operator`，SecretRef 与任一 Tenant CONTROL key 分离，签名目标必须精确等于登记的 `relay_operator_api_origin` 加 operator route。Tenant lifecycle 只能借此 identity 创建 Community，创建后的业务管理立即切换到该 Tenant CONTROL identity（SS-BUZ-OPERATOR）。
- `private_key_secret_ref` 只存 locator，不存私钥明文或平台自制 ciphertext；locator 指向该 Tenant OpenBao namespace 内 KV v2 的具体版本，签名只在 Core signer 内存中完成（DD-72）。
- Resource `home_workspace_id` 与 ActionExecution `workspace_id` 是不同维度。Tenant/成员/全局 Catalog 等 `workspace_rule=TENANT_ONLY` 的管理动作显式使用 `workspace_id=NONE`；Workspace 顶层动作使用受校验 Workspace；Workspace 内工具/模型 child action 继承父 operation 的 Workspace，即使目标 Resource 是 `home_workspace_id=NONE`。不能从 Resource home、模型名或字段缺失推断执行 scope（DD-54）。
- Workspace scoped Action 先要求 Workspace `ACTIVE`，再按 actor kind 执行 scope guard：HUMAN 必须有 active WorkspaceMembership，或 fresh SpiceDB Check 证明 workspace `manage`；AGENT 必须同时匹配 active AgentInstallation/AgentPrincipal、fresh workspace `discover`、Buzz Channel roster 与 active ChannelAgentBinding。该 guard 在 Resource/Asset permission Check 之前执行；因此 owner/Resource permission、频道 event 或 ToolBinding 都不能单独绕过 Workspace 边界。暂停/恢复等生命周期 Action 从 Tenant scope 指向 Workspace，不借用被暂停的 Workspace scope（DD-46/50）。
- CollaborationUserState 只属于一个 active HUMAN Principal。`workspace_preferences` 的 key 必须是同 Tenant 且该 HUMAN 可发现的 Workspace；`read_contexts` 只接受该 HUMAN 当前可读 Workspace 内的 Channel ID、`msg:<Buzz event id>` 或 `thread:<Buzz root event id>`。BFF 每次写入从 PlatformSession 取 principal/scope，不接受 Browser 自报 Principal。撤销 Workspace membership 后立即禁止读写该 scope，保留状态仅用于恢复成员关系后继续使用。
- buzz-web 原有 kind:30078 NIP-44 self-encryption 需要浏览器 signer（SF-WEB-05）；一期 Browser 无 Nostr 私钥，因此该路径固定停用。CollaborationUserState 是平台用户状态，不伪装为服务端 NIP-44 兼容层（DD-40）。

### ExecutionContext

```text
ExecutionContext(
  tenant_id,
  workspace_id | NONE,
  authenticated_principal_id,
  acting_principal_id,
  initiating_human_principal_id | NONE,
  session_id | NONE,
  agent_invocation_id | NONE,
  delegation_id | NONE,
  operation_id,
  correlation_id
)
```

ExecutionContext 只能由 Platform Core 从 session、route、Resource、binding kind/ID/generation 与 runtime identity 构造。浏览器、prompt、Agent 参数与 callback 中的 scope 只是待校验输入。

### Frontend Presentation Context

```text
FrontendPresentationContext(
  theme_preference[LIGHT|DARK|SYSTEM],
  resolved_theme[LIGHT|DARK],
  locale[EN|ZH_CN]
)
```

- `theme_preference` 与 `resolved_theme` 由 Buzz `ThemeProvider` 产生；组件只能消费 `resolved_theme`，不能再解析系统主题或持久化第二份偏好。
- `locale` 由 Buzz 的 `resolveLocale` 产生：浏览器首选语言为 `zh*` 时取 `ZH_CN`，其他取 `EN`。一期只允许这两个正式 locale；新增 locale 必须先进入 Buzz `AppLocale + messages`，再供组件映射。
- `theme_preference` 是当前设备的 `buzz-web-theme` localStorage 偏好，不宣称跨设备同步；`locale` 没有用户偏好字段或运行时切换器，一期不显示语言设置。React App 内的登录回跳、无 Tenant、加载和错误状态仍在同一 ThemeProvider/AppLocale 下；外部 IdP 页面不属于 Buzz Web surface（DD-53）。
- 所有 Buzz host-native 业务页、管理页、弹窗、空状态、错误、审批和任务状态都使用 Buzz CSS variables 与 `MessageKey/t`；remote module 使用 Host API presentation context 与 namespaced catalog；后端只返回稳定状态码和结构化参数，不返回最终界面文案。
- 平台自有日期、时间、数字与相对时间使用 `getLocale()`；用户输入的消息、Resource 名称、文件名、Agent 输出和组件业务正文保持原文，不送入 `t()` 也不自动翻译。native error 原文只作为受权 evidence，UI 状态使用稳定 code 对应的 MessageKey（DD-53）。
- source-bound surface 只接收其固定协议能够证明的 theme/locale 映射。不能接收 FrontendPresentationContext 的组件原生 UI 不得嵌入 Buzz Web。

## 3. 组件、Resource 与 Asset

```text
ComponentDefinition(
  type_key, catalog_tenant_id, class[PLATFORM|APPLICATION],
  platform_port_key[CORE_INTERNAL|IDENTITY_EDGE|AUTHORIZATION|APPROVAL_WORKFLOW|METERING_BILLING|AI_GATEWAY|MATERIALIZATION|SECRET_STORE|COLLABORATION_RELAY|NONE],
  status[ACTIVE|RETIRED]
)

ComponentRelease(
  id, component_type_key, version,
  source_project, source_commit, manifest_schema_version,
  component_package_digest,
  host_api_range | NONE, adapter_protocol_range | NONE,
  binding_config_schema_version, binding_config_schema_digest,
  frontend_delivery, execution_connector,
  target_resource_type_keys[],
  resource_type_definitions[], action_definitions[], tool_definitions[],
  business_observability_contract,
  frontend_presentation_contract,
  adapter_build_ref | NONE,
  status[REGISTERED|APPROVED|REJECTED|REVOKED]
)

ComponentPackage(
  component_release_id, manifest_digest, package_digest,
  web_artifacts[path, media_type, digest][], web_artifact_set_digest | NONE,
  backend_delivery[EXTERNAL_SERVICE|BUILD_BOUND|NONE]
)

FrontendDelivery(
  mode[HOST_NATIVE|REMOTE_MODULE|SOURCE_BOUND_PROTOCOL|NONE],
  source_entry_url | NONE, entry_digest | NONE,
  extension_slots[], host_api_range | NONE
)

ExecutionConnector(
  mode[PLATFORM_PORT_CLIENT|REMOTE_ADAPTER|EMBEDDED_DRIVER|PROTOCOL_PEER|INTERNAL],
  adapter_protocol_range | NONE, action_token_audience | NONE
)

PlatformBuildInfo(
  subject[BUZZ_WEB|CORE|WORKER], build_id,
  host_api_version | NONE, adapter_protocol_versions[],
  driver_registry_keys[], platform_port_keys[], reported_at
)

FrontendPresentationContract(
  integration_mode[BUZZ_NATIVE|HOST_API_REMOTE|LAUNCH_MAPPED|NONE],
  theme_mapping, locale_mapping,
  theme_change_policy[LIVE|NEXT_LAUNCH|NONE],
  locale_resolution_policy[BROWSER_RESOLVED|NEXT_LAUNCH|NONE]
)

BusinessObservabilityContract(
  correlation_sources[OPERATION_ID|NATIVE_REF|GATEWAY_REQUEST_LOG],
  business_state_sources[CORE_ACTION|BUZZ_EVENT|TEMPORAL|NATIVE_STATUS|AGENTGATEWAY|OPENMETER],
  aggregation_dimensions[], redaction_policy
)

ResourceTypeDefinition(
  type_key, incremental_contracts[],
  llm_gateway_contract[REQUIRED|NOT_USED],
  transfer_formats[], tenant_delete_action_key | NONE,
  status
)

TransferFormat(
  format_key, format_version,
  export_action_key, import_action_key,
  digest_rule, package_media_type
)

IncrementalContract(
  contract_key,
  role[NATIVE_INTERNAL|MATERIALIZATION_SOURCE|MATERIALIZATION_TARGET|VERSIONED_PUBLICATION],
  engine[NATIVE|COCOINDEX|VERSIONED_REPUBLISH],
  stable_key_rule, revision_rule, delete_semantics,
  reconciliation_trigger, readiness_semantics
)

PlatformProviderBinding(
  id, catalog_tenant_id, platform_port_key,
  component_type_key, component_release_id,
  provider_target(mode[REMOTE_SERVICE|BUILD_BOUND_DRIVER|CORE_INTERNAL], ref),
  normalized_config, config_digest,
  call_identity_mode[INSTANCE_SERVICE|INTERNAL],
  secret_refs[], active_projection_generation | NONE,
  state[PROVISIONING|ACTIVE|UPGRADING|DISABLING|DISABLED|ERROR], version
)

ApplicationBinding(
  id, tenant_id, workspace_id | NONE,
  component_type_key, component_release_id,
  adapter_service_ref | NONE, native_instance_ref,
  normalized_config, config_digest,
  isolation_mode[DEDICATED_INSTANCE|NATIVE_TENANT|NAMESPACE|RESOURCE_FILTER|RESOURCE_INSTANCE],
  native_scope_ref | NONE,
  call_identity_mode[END_USER_TOKEN|TENANT_SERVICE|INSTANCE_SERVICE|INTERNAL],
  secret_refs[], active_projection_generation | NONE,
  state[PROVISIONING|ACTIVE|UPGRADING|DISABLING|DISABLED|ERROR], version
)

ComponentRuntimeProjection(
  binding_kind[PLATFORM_PROVIDER|APPLICATION], binding_id,
  generation, component_release_id,
  normalized_manifest_digest,
  adapter_contract_digest | NONE,
  frontend_entry_digest | NONE, served_entry_path | NONE,
  state[PENDING|ACTIVE|ERROR|REVOKED]
)

Resource(id, tenant_id, type_key, home_workspace_id | NONE,
         owner_principal_id, component_type_key,
         application_binding_id | NONE,
         native_type, native_id, state, version)

Asset(id, tenant_id, resource_id, type_key,
      owner_principal_id, producer_principal_id | NONE,
      native_ref, state, version)

ExportPackage(asset_id, source_resource_id, source_asset_id | NONE,
              source_object_version, format_key, format_version,
              content_digest, export_operation_id,
              state[CREATING|READY|REVOKED|DELETED|ERROR])

MaterializationBinding(
  resource_id, source_resource_id, target_resource_id,
  source_contract_key, target_contract_key,
  pipeline_key, pipeline_version,
  coco_environment_ref, coco_app_name,
  state[ACTIVE|PAUSED|DELETING|DELETED|ERROR], version
)

ComponentProjection(
  id, resource_id, target_application_binding_id,
  native_type, native_id, source_version,
  state[PENDING|ACTIVE|ERROR|REVOKED]
)

SemanticModel(resource_id, stable_slug,
              current_published_version_asset_id | NONE, status)
SemanticModelVersion(asset_id, semantic_model_resource_id, ordinal,
                     mdl_digest, profile_secret_ref,
                     strict_mode[TRUE], row_limit, config_hash,
                     state[DRAFT|PUBLISHED|RETIRED])
SemanticModelRuntimeProjection(version_asset_id,
                               wren_application_binding_id,
                               gateway_resource_ids[], config_hash,
                               state[PENDING|ACTIVE|ERROR|REVOKED])

IngressCheckpoint(source_key, tenant_id | NONE, cursor, updated_at)

WorkspaceDriveBinding(workspace_id, drive_space_resource_id, state, version)

FileReference(
  resource_id, asset_id | NONE, node_uuid, revision,
  display_name, media_type
)

DocumentSession(
  id, operation_id, tenant_id, workspace_id,
  actor_principal_id, initiating_human_principal_id | NONE,
  file_reference, application_binding_id,
  requested_mode[VIEW|EDIT], admitted_mode[VIEW|EDIT],
  wopi_session_id, token_revocation_key,
  launch_theme[LIGHT|DARK], launch_locale[EN|ZH_CN],
  base_revision, base_modified_at,
  state[ADMITTED|OPENING|OPEN|DIRTY|SAVED|READ_ONLY|CONFLICT|REVOKED|CLOSED|UNKNOWN|FAILED],
  last_wopi_correlation_id | NONE, result_revision | NONE,
  expires_at, version
)
```

- Digest 规则全局统一，所有 `*_digest` 字段与 `integrity` 值都用它：字节流直接 `sha256`；结构化对象先按 canonical JSON（键排序、无无意义空白、UTF-8）序列化再 `sha256`。路径中使用十六进制表示，浏览器 `integrity` 属性使用 `sha256-<base64>`。没有该规则就无法复算 `config_digest`/`normalized_manifest_digest`，也无法做 Subresource Integrity。
- `PlatformBuildInfo` 是当前已部署主体的能力事实，兼容性判定的"当前一侧"只从它读取：Catalog 批准 release 要求 `host_api_range` 与已部署 Buzz Web 的 `host_api_version` 相交、`adapter_protocol_range` 与 Core/Worker 的 `adapter_protocol_versions` 相交、`EMBEDDED_DRIVER`/`INTERNAL` 的键在 `driver_registry_keys` 内、`platform_port_key` 在 `platform_port_keys` 内；BFF 返回 Component 快照时按请求方自报并经校验的 `host_api_version` 过滤，滚动发布期间不同版本的 Browser 各自只看到自己能加载的 generation。缺少 `PlatformBuildInfo` 时 release 不得批准、快照不得下发。
- `adapter_build_ref` 固定 `REMOTE_ADAPTER` 实现的构建产物 digest。adapter 在 handshake 中必须出示与 release 登记值一致的 `adapter_build_ref`；`source_commit` 只固定上游项目，不能证明 Kailo 自建 adapter 的字节。不一致时 binding 停在 `PENDING/ERROR`。
- Resource 是平台持久化的治理根；native ID 永不作为平台 API 的裸标识。
- Resource 和每个已登记 Asset 都显式保存一个 active HUMAN owner，不使用条件性 `owner_override`。
- Resource、home Workspace、ApplicationBinding、owner 及所有 relationship subject 必须同 Tenant；Asset、父 Resource、owner/producer 也必须同 Tenant。Core 在写业务事实和 SpiceDB relationship 前同时校验，不依赖 schema 自动推断这个不变式。Core 原生 Resource 的 `application_binding_id=NONE`；Application Component 所属 Resource 必须指向同 Tenant 的 active ApplicationBinding。
- `home_workspace_id=NONE` 只表示 Tenant-level Resource；非空时 Workspace 必须同 Tenant。Workspace 暂停保留 Resource/Asset、owner 与 relationships，但 scope guard 拒绝普通读写、Agent trigger、Tool discovery 和新 Workflow；恢复完成后重新开放。当前 Workspace 不含 `DELETING/DELETED` 状态，也不注册 delete Action（DD-46）。
- 任何 SpiceDB relationship、ApplicationBinding、MaterializationBinding、FileReference、ToolBinding 或 native object 引用都不得跨 Tenant。PlatformProviderBinding 不引用业务 Resource/Asset。跨 Tenant 不是 `share` permission 的一种变体；它只能通过 DD-42 的两段 export/upload/import 边界生成新对象。
- 未持久登记的文件子项、知识块和临时 SQL 行留在 native Resource 内，不是 Platform Asset。
- Agent/service 只能是 producer；新 Asset 的 owner 由 initiating HUMAN 或已批准的 owner-transfer action 确定。
- ExportPackage 是源 Tenant 内的 Asset 扩展，必须与 source Resource/Asset、export operation 及 HUMAN owner 同 Tenant。package 只在 `READY` 时可下载；导入时目标 Tenant 只信任新 session 上传的 bytes、已登记 `format_key/version` 与重算 digest，不信任 package 内的 Tenant、owner、ACL、native ID、URL 或 token。目标 Resource/Asset 由目标 Tenant `create` 准入重新创建；不建立到源对象的平台外键或授权关系。
- 应用不必原生具有 Kailo Tenant/Workspace；ApplicationBinding 必须固定一种源码可证明的隔离方式，解析失败时拒绝而不是回退默认实例。`RESOURCE_INSTANCE` 表示一个平台 Resource/不可变版本独占一个固定 native runtime context；Wren 的单 project `ServeContext` 是一期唯一实例。
- 每个部署只有一个 `catalog_tenant_id`，它引用正常受治理的 Platform Catalog Tenant。ComponentDefinition/Release 是该 Tenant 管理的全局技术元数据，不是可跨 Tenant 共享的业务 Resource；release 登记、批准、撤销与 artifact 访问均以该 Tenant 的 platform-admin Principal 进入 Governed Action。业务 Tenant 的 Binding 只能引用 `APPROVED` release，不能修改 release 或上传代码。
- ComponentDefinition 只表达稳定类型与分类。`class=PLATFORM` 时 `platform_port_key` 必须是 Core 已登记端口；`class=APPLICATION` 时必须为 `NONE`。ComponentRelease 发布后不可变；source commit、manifest schema、Host/Adapter Protocol range、Resource/Action/Tool、治理合同、Web artifact 任一变化都创建新 release。ComponentPackage 只携带 manifest 与 digest 固定的 Web artifacts，不包含后端 executable；远程服务独立交付，`BUILD_BOUND` 代码必须已存在于当前 Core/Worker 构建。
- PlatformProviderBinding 只能 pin `class=PLATFORM` 的 approved release，且 `platform_port_key` 必须与 Definition 一致；它属于 Platform Catalog scope，不建立业务 native scope、Resource 或 Asset。`REMOTE_SERVICE` 固定独立 service ref；`BUILD_BOUND_DRIVER|CORE_INTERNAL` 的 ref 必须出现在当前主体已编译 registry，否则拒绝 binding。每个 port 同时只有一个 active generation，新调用使用新 generation，已开始的 Action/Workflow 保持旧 generation。
- ApplicationBinding 必须 pin `class=APPLICATION` 的 approved release。`ACTIVE` 要求 native scope 已对账、七项治理投影与 Business Observability 已闭合；有 Web surface 时还要求 FrontendPresentationContract 与 artifact digest 验证通过。`REVOKED` release 拒绝新 binding/upgrade，并触发既有 binding fail-closed 停用，不静默切换到其他版本。
- Binding 的 `normalized_config` 只保存按该 release 固定 schema 规范化后的非敏感值，`config_digest` 参与 projection generation；credential、provider key、PAT 与 cookie key 只能成为具名 `SecretRef`。Upgrade 时新 schema 不能验证旧 config 或 SecretRef audience 就保持旧 generation 或 fail closed，不在 Core 执行 manifest 提供的任意迁移代码。
- ComponentRuntimeProjection 是 PlatformProviderBinding 或 ApplicationBinding 的可执行快照。升级先生成新 `PENDING` generation，校验 port/adapter、native scope、Action/Tool/route 与 Web artifact 后原子切换 `active_projection_generation`；新调用只取新 generation，已开始的 ActionExecution/Workflow 固定旧 release/generation。回滚同样生成新 generation，不改写历史执行。
- 普通 Application Component 至少声明一个 ResourceType。能力型 Application Component 只有在“不产生自己的长期 ownable 数据、所有 action 都指向 `target_resource_type_keys[]` 中既有类型、短期 session 不升格为 Resource”三个条件同时成立时，才允许 `resource_type_definitions=[]`；ONLYOFFICE 是该规则的一期实例。
- 前端交付与 native 项目是否前后端分离无绑定关系。`HOST_NATIVE` 使用随 Component Host 发布的通用资源/表单/任务视图；`REMOTE_MODULE` 的 source URL只在 Catalog 登记时取件，运行时从 digest 固定的同源 `served_entry_path` 加载；`SOURCE_BOUND_PROTOCOL` 只使用源码证明的固定协议 launch；`NONE` 不提供组件页面。Cells、WeKnora、Wren 等 monolith 可保持原后端，由 remote adapter 接入并选择 `HOST_NATIVE`，不要求拆出原生前端。
- `REMOTE_MODULE` 与宿主同窗口运行，属于平台管理员批准的宿主信任代码，不是权限或故障隔离边界。它只能通过 versioned Host API 登记 `MANAGEMENT_PAGE / RESOURCE_VIEW / CHAT_ATTACHMENT_ACTION` 三个一期扩展槽；BFF 对每次 query/action 仍按当前 session fresh准入，模块不能取得 native management credential、ActionToken signing key、SpiceDB/OpenMeter/Temporal/AgentGateway admin credential 或完整宿主 store。
- `SOURCE_BOUND_PROTOCOL` 只允许固定 source commit、固定 origin、固定 BFF descriptor schema 的协议 surface；ONLYOFFICE WOPI editor page 是一期实例。`PLATFORM_PORT_CLIENT` 只允许 `class=PLATFORM` 且绑定 Core 已知 port；`EMBEDDED_DRIVER` 只允许源码本身提供 library runtime且没有外部管理 API；`PROTOCOL_PEER` 只允许双方固定协议已相交；其他 Application Component 统一使用独立 `REMOTE_ADAPTER`，禁止上传本机 executable 给 Core 启动。
- 每个非 `NONE` FrontendDelivery 必须声明 FrontendPresentationContract。Host-native 固定 `BUZZ_NATIVE + theme LIVE + locale BROWSER_RESOLVED`；remote module 固定 `HOST_API_REMOTE + theme LIVE + locale BROWSER_RESOLVED`并同时提供 `en/zh-CN` namespaced messages；ONLYOFFICE 固定 `LAUNCH_MAPPED + theme/locale NEXT_LAUNCH`。缺少任一语言 catalog、主题 token 合同或错误 code 映射时，该 Web surface 不能 active。
- `incremental_contracts[]` 至少含一个 `NATIVE_INTERNAL` 或 `VERSIONED_PUBLICATION` 合同；可再登记 `MATERIALIZATION_SOURCE/TARGET`。同一 ResourceType 可同时保留 native 内部派生并参与跨 Resource 物化，不能用一个单值 mode 混淆两层职责。`COCOINDEX` 只用于跨 Resource endpoint；整体配置/模型换代使用 `VERSIONED_REPUBLISH`。
- `transfer_formats[]` 默认为空，表示该 ResourceType 不支持跨 Tenant 转移。每个条目必须把一个固定 format/version/digest rule 同时绑定已注册的 `RESOURCE_EXPORT` 和 `RESOURCE_IMPORT` ActionDefinition；缺任一端时不能注册为可转移格式。`tenant_delete_action_key=NONE` 表示该 ResourceType 的 native 对象不能由 `ComponentTaskWorkflow(kind=TENANT_LIFECYCLE)` 证明删除；存在此类 active Resource 时 Tenant 不得进入 `DELETED`。
- 当前固定基线下，全部首批 ResourceType 的 `transfer_formats=[]` 且 `tenant_delete_action_key=NONE`：前者没有在 `02` 登记成对 TransferFormat SOURCE_SEAM，后者受 GAP-LCM-01 阻断。这是当前 Catalog 的确定注册结果；不将“未注册”改写成“上游全项目永久不具备”。
- MaterializationBinding 引用的 source/target contract 必须分别属于两端 ResourceType 的 active `MATERIALIZATION_SOURCE/TARGET` 合同，且两者 engine 均为 `COCOINDEX`；pipeline version 改变时创建新的 binding version，不原地改变 item identity。
- MaterializationBinding 是 `Resource(type_key=materialization_binding)` 的一对一扩展；Tenant、home Workspace、HUMAN owner 和 `execute/manage/audit` relationship 只存 Resource，不在扩展中复制。Binding Resource、两端 Resource 与全部 owner 必须同 Tenant；创建者同时具备源 `read/export` 与目标 `create/manage`。创建、变更 source/target/pipeline 或清除目标副本时，initiating HUMAN 不是对应端 owner 的，必须取得该端 owner 批准；同一 HUMAN 同时拥有两端时只产生一个 owner 决策。
- MaterializationBinding 只登记跨资源关系、pipeline 版本及 CocoIndex 持久状态归属。`coco_environment_ref` 必须解析到稳定的**绝对** LMDB `db_path`，`coco_app_name` 固定由 binding Resource ID 与 pipeline version 派生；同一 active version 的全部 run 必须回到该 Environment/App namespace。逐项 memo、目标 owner、pending state 和删除跟踪属于该 CocoIndex App 权威，不复制到 Core。CocoIndex SDK 在 `db_path` 未设置时静默回落到相对路径 `./coco_state` 而不报错（SF-CIX-04），因此“状态目录不可达时 run 失败，禁止换空目录后把全部 source 当作新增”是 Core runner 必须自行强制的前提：Core 始终显式传入该绝对路径，并在 run 前校验目录存在且已初始化，不依赖 SDK 默认值；pipeline version 变化使用新 App namespace。purge 固定为两步且顺序不可颠倒：先以同一 App 运行一次 desired set 为空的 update（root 不再声明任何 item，由 reconcile 对每个 tracked item 发出 Delete 并等待 target 成功），确认该 run 无 error 且 tracking 已空，再调用 `drop_state` 清理本地状态。Rust SDK 的 `drop_state` 在 env 级 target-state provider registry 为空时只清 LMDB、不向 target 发 Delete，单独调用会留下无法再被对账的孤儿 Knowledge（SF-CIX-06）。`coco_environment_ref` 为每个 MaterializationBinding 独立的绝对 db_path，不把多个 binding 的 App 叠进同一 LMDB environment：`drop_state` 不释放 named-DB 目录项，App 名随 pipeline version 单调增长会耗尽 environment 的 max_dbs（SF-CIX-07）。
- `PAUSED` 停止新的源读取与目标变更但保留已物化内容；`DELETING` 只由单独的清除 Governed Action 进入，不把暂停解释为删除。
- ComponentProjection 记录平台 Resource 在第二组件中的受控投影。例如 `llm_route` 的 AgentGateway ConfigResource 是原生权威，WeKnora model row 与 Codex provider 配置是其投影。
- Wren SemanticModel 是 Tenant 级 Resource，发布版本是具有 HUMAN owner 的不可变 Asset。每个 PUBLISHED version 固定完整 `mdl.json` digest、数据库 profile SecretRef、`strict_mode=true` 与行限；RuntimeProjection 一对一绑定该 version 的 Wren `RESOURCE_INSTANCE` 和 AgentGateway Streamable HTTP MCP target。运行时不能切 project/profile，发布新模型不改写旧 version 或旧 query evidence（SF-WRN-01/02/03、DD-12）。
- IngressCheckpoint 只保存外部持久流的恢复游标。重复读取仍由源稳定 ID 的业务唯一约束去重，不能把游标当成 exactly-once 保证。
- `llm_route` 是 `PLATFORM` ResourceType：具有 HUMAN owner、可选 home Workspace、`discover/execute/manage/audit` 权限和 AgentGateway native ID；组件不得把上游 URL/API key 另建为业务 Asset。
- `cells_drive_space` 是 Cells 组件登记的 DriveSpace ResourceType；文件/文件夹默认只是该 Resource 的 native child，不存在 `cells.file` ResourceType。WorkspaceDriveBinding 只选择 Workspace 默认 DriveSpace Resource，不复制 Cells workspace/root。FileReference 的 `revision` 固定为 Cells VersionId，ETag/ContentHash 只作校验证据，mtime 单独进入 `DocumentSession.base_modified_at`。FileReference 必须解析到同 Tenant、同 Workspace 可访问的 `cells_drive_space` Resource 和确定 revision；文件已经提升为 Asset 时 `asset_id` 必填并成为细粒度授权目标，未提升时授权目标是父 DriveSpace Resource。聊天 event、审批、任务与 citation 均不得携带临时 download URL、DocumentServer token 或 editor config。
- DocumentSession 是 PROTOCOL execution 的短期记录，不拥有 owner；owner 仍取授权目标 Cells Resource/Asset 的 active HUMAN。请求 `VIEW` 检查 `read`，请求 `EDIT` 检查 `update`；EDIT admission 还要求 `file_reference.revision` 在 fresh NodeVersions 中是 head，历史 revision 只能 VIEW。Cells CheckFileInfo/GetFile 始终按 session 的 base revision 返回 FileInfo 与内容，不静默换 latest；EDIT 的 PutFile 只写同一 node。`wopi_session_id=DocumentSession.id`，同时作为 launch `usid`；`token_revocation_key` 绑定同一 session。`SAVED` 只有 Cells WOPI PutFile evidence 已被接受且 NodeVersions 确认 `result_revision` 时成立；写入结果不明进入 `UNKNOWN`。launch theme/locale 是该 editor session 的稳定快照，不因宿主切换而静默重载。

## 4. Governed Action 模型

```text
ActionDefinition(
  action_key, component_type_key, target_type,
  tenant_rule[SESSION_TENANT|TARGET_TENANT|INHERIT_PARENT],
  workspace_rule[TENANT_ONLY|WORKSPACE_REQUIRED|TARGET_HOME_WORKSPACE|INHERIT_PARENT],
  permission,
  execution_mode[SYNC|PROTOCOL|TEMPORAL],
  confirmation_mode[NONE|EXPLICIT|APPROVAL],
  approval_policy_id | NONE,
  workflow_type | NONE,
  capacity_policy[NONE|PLATFORM_SLOT|NATIVE],
  capacity_pool_key | NONE,
  quota_policy[NONE|CHECK|STRICT_RESERVATION],
  meters[], result_exposure_policy,
  audit_policy, business_observability_policy, version, status
)

BusinessObservabilityPolicy(
  correlation_mode[OPERATION_REF|OPERATION_NATIVE_REF],
  progress_source[NONE|TEMPORAL|NATIVE],
  terminal_source[SYNC_RESULT|TEMPORAL|NATIVE],
  usage_source[NONE|DRIVER|GATEWAY_DURABLE_USAGE|OPENMETER],
  cost_source[NONE|GATEWAY_DURABLE_USAGE|OPENMETER],
  redaction_policy
)

ApprovalPolicy(
  id, tenant_id, action_key, target_type,
  role_requirements[{selector[RESOURCE_APPROVER|WORKSPACE_ADMIN|TENANT_ADMIN], min_distinct}],
  owner_requirement[NONE|TARGET_OWNER|ALL_AFFECTED_OWNERS],
  self_approval[ALLOW|DENY],
  expires_in, version, status
)

ActionDecision(
  operation_id, context, action_key, target_id, parameter_hash,
  scope_decision, authorization_decision, delegation_decision,
  approval_decision, capacity_decision, quota_decision, audit_decision,
  zed_token | NONE, reason_code
)

ActionExecution(
  id, operation_id, tenant_id, workspace_id | NONE,
  action_key, action_version, initiator_principal_id,
  actor_principal_id, target_id, parameter_hash,
  component_binding_ref[kind,id] | NONE,
  component_release_id | NONE, component_projection_generation | NONE,
  delegation_id | NONE, approval_workflow_id | NONE,
  temporal_workflow_id | NONE,
  gate_state[EVALUATING|WAITING|ALLOWED|DENIED|REVOKED|EXPIRED],
  dispatch_state[NOT_DISPATCHED|DISPATCHED|ABORTED|UNKNOWN],
  outcome_ref | NONE, correlation_id
)

TenantLifecycleSnapshot(
  id, tenant_id, action_execution_id, tenant_version,
  resource_owner_versions[], asset_owner_versions[],
  component_binding_refs_and_versions[], inventory_digest,
  irreversible_dispatch_started[TRUE|FALSE], state
)
```

- 每个治理入口都有 `operation_id` 和 ActionDecision。
- 组件动作在准入时从 active ComponentRuntimeProjection 固定 `binding_kind + binding_id + release_id + generation`；dispatch、Workflow、ExternalExecution、UsageEvent 与 AuditEvent 沿用同一快照。运行中 upgrade/rollback 不改变该动作解析到的 schema、adapter、meter 或 redaction。
- 只有 Approval、Temporal、Agent 委托、异步 native task、CapacityLease、QuotaReservation、可重试外部副作用或 UNKNOWN 对账需要 ActionExecution。同请求简单读写只保存 AuditEvent。
- ActionExecution 只保存门禁/调度状态；Temporal 运行终态只在 Temporal/TaskProjection。
- TenantLifecycleSnapshot 是 Tenant 销毁 Workflow 的不可变 Core 清单，只保存 Resource/Asset/owner/binding ID+version 与 digest，不保存业务正文。Tenant 进入 `SUSPENDING` 后禁止创建新 Resource/Asset，才能冻结该 snapshot；Workflow 恢复时以 snapshot 和 ExternalExecution refs 继续，不重新从可变列表计算已批准对象。
- 每个 ActionDefinition 必须显式给出 Tenant rule、Workspace rule、permission、execution、approval、capacity、quota/meter、result exposure、audit schema 和 observability policy，缺任一项不得 active。progress/usage/cost 可显式 `NONE`，但 correlation、terminal source 与 redaction 不得缺失。`GATEWAY_DURABLE_USAGE` 只有 SS-AGW-USAGE 已闭合且写入获得持久确认时可登记；原始 AgentGateway request log 只能作为 native observability evidence。
- `TENANT_ONLY` 只用于 Tenant 自身、TenantMembership 和 Tenant 级管理意图；`WORKSPACE_REQUIRED` 使用当前 active Workspace；`TARGET_HOME_WORKSPACE` 还要求执行 Workspace 等于目标 home Workspace；`INHERIT_PARENT` 只用于同一 operation 的内部 child action，并复制父 Workspace（含 NONE）。任何规则都要校验 target/binding 与 execution Tenant 相同。Tenant-level Resource 被 Workspace Agent消费时，child action 使用 `INHERIT_PARENT`，不因 `home_workspace_id=NONE` 丢弃 Workspace（DD-54）。

## 5. 固定 SpiceDB schema

平台 schema 只含 `principal/tenant/workspace/resource/asset`。组件注册不新增 object type。

```zed
definition principal {}

definition tenant {
  relation member: principal
  relation admin: principal
  relation creator: principal
  relation auditor: principal
  permission discover = member + admin
  permission create = creator + admin
  permission manage = admin
  permission audit = auditor + admin
}

definition workspace {
  relation tenant: tenant
  relation member: principal
  relation admin: principal
  relation creator: principal
  relation auditor: principal
  permission discover = member + admin + tenant->manage
  permission create = creator + admin + tenant->create + tenant->manage
  permission manage = admin + tenant->manage
  permission audit = auditor + admin + tenant->audit
}

definition resource {
  relation tenant: tenant
  relation home_workspace: workspace
  relation owner: principal
  relation discoverer: principal
  relation consumer: principal
  relation reader: principal
  relation exporter: principal
  relation creator: principal
  relation editor: principal
  relation deleter: principal
  relation sharer: principal
  relation manager: principal
  relation executor: principal
  relation approver: principal
  relation delegator: principal
  relation owner_transferrer: principal
  relation auditor: principal

  permission discover = owner + discoverer + home_workspace->manage + tenant->manage
  permission consume = owner + consumer + home_workspace->manage + tenant->manage
  permission read = owner + reader + home_workspace->manage + tenant->manage
  permission export = owner + exporter + home_workspace->manage + tenant->manage
  permission create = owner + creator + home_workspace->manage + tenant->manage
  permission update = owner + editor + home_workspace->manage + tenant->manage
  permission delete = owner + deleter + home_workspace->manage + tenant->manage
  permission share = owner + sharer + home_workspace->manage + tenant->manage
  permission manage = owner + manager + home_workspace->manage + tenant->manage
  permission execute = owner + executor + home_workspace->manage + tenant->manage
  permission approve = owner + approver + home_workspace->manage + tenant->manage
  permission delegate = owner + delegator + home_workspace->manage + tenant->manage
  permission transfer_owner = owner + owner_transferrer + tenant->manage
  permission audit = owner + auditor + home_workspace->audit + tenant->audit
}

definition asset {
  relation tenant: tenant
  relation resource: resource
  relation owner: principal
  relation discoverer: principal
  relation consumer: principal
  relation reader: principal
  relation exporter: principal
  relation editor: principal
  relation deleter: principal
  relation sharer: principal
  relation manager: principal
  relation executor: principal
  relation approver: principal
  relation delegator: principal
  relation owner_transferrer: principal
  relation auditor: principal

  permission discover = owner + discoverer + resource->manage
  permission consume = owner + consumer + resource->manage
  permission read = owner + reader + resource->manage
  permission export = owner + exporter + resource->manage
  permission update = owner + editor + resource->manage
  permission delete = owner + deleter + resource->manage
  permission share = owner + sharer + resource->manage
  permission manage = owner + manager + resource->manage
  permission execute = owner + executor + resource->manage
  permission approve = owner + approver + resource->approve
  permission delegate = owner + delegator + resource->delegate
  permission transfer_owner = owner + owner_transferrer + resource->transfer_owner
  permission audit = owner + auditor + resource->audit
}
```

规则：

- 规范 permission 只是 `discover/consume/read/export/create/update/delete/share/manage/execute/approve/delegate/transfer_owner/audit`。Asset 不有 `create`，创建检查父 Resource 的 `create`。
- 尚不存在的顶层 Resource 无法成为 Check 对象；创建顶层 Resource 检查所在 Workspace 的 `create`，Tenant-level Resource 检查 Tenant `create`，创建后再写 Resource owner 与其他 relationships。
- `consume` 只允许 Agent 返回收窄 answer、citation、aggregate 或 derived Asset；原文、文件下载、raw SQL 行和模型定义需要 `read/export`。
- RoleTemplate 只是 Platform Core Catalog 中从角色名到上述关系写入集合的版本化模板，不是第二授权引擎或第二套 assignment 表。角色的授予与撤销、有效 Tenant admin 不得为空，以及部署引导只能 0→1 写入首位 Tenant admin，按 DD-82。
- Resource/Asset owner 是 Core 业务事实，SpiceDB owner 是授权投影；未对账时 owner 审批和 owner-only 高风险动作拒绝。
- WriteRelationships 返回的 ZedToken 传入同一因果链的 `AtLeastAsFresh`检查；无因果 token 且即将发生不可逆外部副作用时使用 `FullyConsistent`。列表可使用低延迟 consistency，但不可代替最终 admission Check。
- `LookupResources` 只用于有明确 resource type、permission、有限 `OptionalLimit` 与 cursor 的发现路径；limit 不得超过服务端 `MaxLookupResourcesLimit`，cursor 只在原请求与原授权 revision 内继续，不跨 revision 复用。Tenant 管理页不以管理员的 `tenant->manage` 展开全 Tenant stream，而先对 Core 自有索引分页，再用 `CheckBulkPermissions` 过滤当前页；最终动作仍单独 fresh Check（SF-SPZ-05）。

## 6. Delegation、Approval 与 Workflow

```text
DelegationGrant(
  id, tenant_id, workspace_id, grantor_principal_id,
  installation_resource_id, agent_principal_id,
  valid_from, expires_at, max_uses | NONE,
  state[ACTIVE|REVOKING|REVOKED|EXPIRED], version
)
DelegationScope(
  delegation_id, action_key,
  target_type, target_id | NONE,
  create_workspace_id | NONE,
  tool_resource_id | NONE,
  result_exposure_policy_id, result_exposure_policy_version
)
DelegationUse(delegation_id, operation_id, first_dispatch_at)

ResultExposurePolicy(
  id, tenant_id, mode[CONSUME_ONLY|READ|EXPORT],
  output_schema_hash, redaction_policy, version, status
)

ActionTokenClaims(
  jti, issuer, audience, issued_at, expires_at,
  tenant_id, workspace_id | NONE,
  operation_id, action_execution_id,
  actor_principal_id, initiating_human_principal_id | NONE,
  agent_principal_id | NONE,
  delegation_id | NONE, delegation_version | NONE,
  action_key, action_definition_version,
  target_type, target_id | NONE,
  normalized_parameter_hash,
  result_exposure_policy_id, result_exposure_policy_version,
  authorization_min_zed_token | NONE
)

ApprovalProjection(
  workflow_id, action_execution_id, tenant_id, workspace_id | NONE,
  target_type, target_id, action_definition_version, parameter_hash,
  policy_id, policy_version,
  affected_owner_refs[{target_type, target_id, target_version,
                       owner_principal_id}],
  role_requirements[], self_approval,
  decisions[{approver_principal_id, decision[APPROVE|DENY], decided_at,
             audit_event_id}],
  status[REQUESTED|WAITING|APPROVED|DENIED|EXPIRED|CANCELLED|CONSUMED|INVALIDATED],
  expires_at, consumed_at | NONE
)

WorkflowRef(
  workflow_id, run_id | NONE, workflow_type, workflow_version, kind | NONE,
  tenant_id, workspace_id | NONE, operation_id, action_execution_id,
  parent_workflow_id | NONE, resource_id | NONE,
  projection_state[PENDING_START|RUNNING|TERMINAL|UNKNOWN], version
)

ExternalExecution(
  id, operation_id, workflow_id, action_execution_id,
  tenant_id, workspace_id | NONE,
  component_binding_ref[kind,id], component_binding_version,
  component_release_id, component_projection_generation,
  native_type, native_id | NONE,
  idempotency_key, request_digest,
  native_status | NONE,
  platform_status[PENDING_DISPATCH|RUNNING|SUCCEEDED|FAILED|CANCELLED|UNKNOWN],
  cancel_capability[SUPPORTED|UNSUPPORTED],
  last_observed_at | NONE, terminal_at | NONE
)

TaskProjection(
  workflow_id, run_id | NONE, workflow_type, workflow_version, kind | NONE,
  tenant_id, workspace_id | NONE, operation_id, action_execution_id,
  parent_workflow_id | NONE, initiator_principal_id, actor_principal_id,
  resource_id | NONE,
  status, progress | NONE, waiting_reason | NONE,
  external_execution_ids[], approval_workflow_id | NONE,
  usage_event_ids[], asset_ids[], audit_event_ids[],
  trace_refs[], observation_gap[TRUE|FALSE], updated_at
)
```

- Agent 允许是 `AgentPrincipal permission ∩ grantor 当前可委托 permission ∩ DelegationScope ∩ Agent/Tool binding ∩ ResultExposurePolicy`。
- DelegationGrant、Installation、AgentPrincipal 和 Workspace 必须同 Tenant/Workspace，grantor 必须是该 Tenant 的 active HUMAN。DelegationScope 不含通配：`action_key` 必填；操作既有对象时 `target_id` 必填且 `create_workspace_id=NONE`，创建动作时 `target_id=NONE` 且 `create_workspace_id` 必须等于 Grant 的 Workspace。Tool 存在时 `tool_resource_id` 也固定。任何不满足这一互斥结构的 scope 拒绝持久化。
- `max_uses` 在真实首次 dispatch 前以 `(delegation_id, operation_id)` 唯一 DelegationUse 原子消费；同 operation retry 不重复计数。达到上限、过期或 `REVOKING` 后新 dispatch 立即拒绝，已发生的 DelegationUse/Audit 保留。
- ResultExposurePolicy 是最大暴露边界：`CONSUME_ONLY` 只允许 answer/citation/aggregate/derived Asset，`READ` 仍要求目标 `read`，`EXPORT` 仍要求目标 `export`；三者还与 ToolDefinition `output_schema_hash` 和 ActionDefinition redaction 求交，不能扩大 permission。
- ActionTokenClaims 只用于跨网络/进程边界，不持久化为授权权威。接收 PEP 必须校验签名、issuer、精确 audience、短 expiry、scope/actor/delegation/action/target/operation，重新规范化参数并比对 hash，再以 `authorization_min_zed_token` 至少同样新鲜或 `FullyConsistent` 做 scope/permission/delegation fresh Check。ActionToken 固定使用非对称签名（`ES256` 或 `EdDSA`，算法白名单固定，拒绝 `none` 与 HMAC 家族）：私钥以 KV v2 版本托管于 OpenBao `platform/` namespace、只由 Core 读取并只在内存签名；公钥集合由 Core 导出为 JWKS，经 OpenBao Agent template 渲染成文件供 AgentGateway 以文件引用加载（文件被 watch，轮换无需重启）。`kid` 取该 KV v2 版本号；轮换用重叠窗口——新 `kid` 先进 JWKS 并确认网关已 reload，再切换签名密钥，旧 `kid` 保留不少于 ActionToken 最大有效期后移除。对称密钥方案不可行：验签方在 AgentGateway 进程内，"只由 Core 读取"与"PEP 校验签名"无法同时成立（DD-49/70/71）。
- Approval 不能补足缺失的 permission、Delegation、scope、binding、capacity 或 quota。
- 一个 ActionExecution 最多绑定一个 ApprovalWorkflow。Workflow 中间步需要独立批准时，创建 child ActionExecution 和 child ApprovalWorkflow。
- ApprovalProjection 只由 Temporal history 投影，不接受 CRUD 改状态。`affected_owner_refs` 在请求时从 Core Resource/Asset 的 `owner_principal_id + version` 与已对账 SpiceDB owner relationship 冻结；两者不一致时请求不启动。`ALL_AFFECTED_OWNERS` 要求列表中每个不同 active HUMAN owner 各有 APPROVE，`TARGET_OWNER` 要求目标 active HUMAN owner APPROVE；`role_requirements` 的每项分别满足其 `min_distinct`。同一 HUMAN 在多个要求中可同时计数，但只产生一个决定。
- approver 必须是 active HUMAN。每个 approver 对同一 workflow 只能写一个不可变决定；决定以 Temporal Update 提交（Update ID = `<approval_workflow_id>:<approver_principal_id>`，Server 侧去重）：Validator 只做确定性状态判断（已终结、重复/冲突决定、过期、approver 在冻结名单内），权限类检查由 handler 体内的 Activity 完成（SF-TSDK-09）；重复同值 Update 幂等，冲突值拒绝。任何通过当前 selector/owner 校验的 `DENY` 使 Workflow 进入 `DENIED`。Agent、SERVICE、过期/失权 Human 的 Update 不形成决定。批准后的主动作只执行一次，进入 `CONSUMED` 后不能复用（DD-47）。
- WorkflowRef 在调用 Temporal Start 前持久化，`workflow_id` 对 ActionExecution 唯一；Start 响应不明时保持 `PENDING_START|UNKNOWN` 并按同一 ID Describe/History，不生成第二个 ID。`run_id` 只从 Temporal observation 回填。TaskProjection 只由 Temporal/history、ExternalExecution 和引用事实生成，控制命令不能直接改 projection（DD-48）。
- ExternalExecution 必须和 Workflow/ActionExecution 的 binding ref 一致；ApplicationBinding 调用还必须同 Tenant/Workspace。binding version 与 request digest 在首次 dispatch 前冻结。每个真实副作用先写稳定 idempotency key；超时且没有 native ID 时进入 `UNKNOWN`，仅按该 ActionDefinition 已登记的 native query/dedupe rule 对账。callback 只能用 ExternalExecution ID 或已绑定 native ID 定位 scope，callback payload 的 Tenant/Workspace 不具权威性。

## 7. Agent 与工具

```text
AgentDefinition(resource_id, stable_slug, display_name,
                current_published_version_asset_id | NONE, status)
AgentVersion(asset_id, agent_resource_id, ordinal,
             persona_identity, instructions,
             runtime_profile_key, model_route_resource_id,
             skill_version_asset_ids[], declared_tool_resource_ids[],
             trigger_defaults, reply_policy, parallelism,
             turn_limits,
             memory_policy(core_write[HUMAN_ONLY|AGENT_WITH_APPROVAL],
                           cold_write[DISABLED|INVOCATION_SCOPED]),
             config_hash,
             state[DRAFT|PUBLISHED|RETIRED])

AgentInstallation(resource_id, workspace_id, agent_resource_id,
                  pinned_version_asset_id, agent_principal_id,
                  runtime_isolation_ref,
                  state[PROVISIONING|ACTIVE|DRAINING|DISABLED|ERROR],
                  active_projection_generation | NONE)
ChannelAgentBinding(workspace_id, installation_resource_id,
                    triggers[MENTION|MANUAL_ASSIGNMENT], status)

RuntimeProfile(key, kind[SERVER_CODEX|LOCAL_ACP|REMOTE_PROVIDER],
               capability_contract, web_availability, status)
AgentRuntimeProjection(installation_resource_id, generation,
                       agent_version_asset_id, runtime_profile_key,
                       model_route_resource_id,
                       gateway_resource_ids[], skill_root_ref | NONE,
                       effective_fields[], config_hash,
                       state[PENDING|ACTIVE|ERROR|REVOKED])
EffectiveField(field_key, requested_value_hash | NONE,
               effective_value_hash | NONE,
               source[AGENT_VERSION|WORKSPACE_POLICY|CHANNEL_BINDING|RUNTIME_DEFAULT],
               reason_code | NONE)

AgentMemoryBinding(installation_resource_id,
                   agent_buzz_identity_binding_id,
                   memory_counterparty_buzz_identity_binding_id,
                   core_head_event_id | NONE,
                   core_head_created_at | NONE,
                   listing_state[COMPLETE|BOUND_EXCEEDED|UNKNOWN],
                   head_state[CONSISTENT|HEAD_AHEAD_OF_RELAY|UNKNOWN],
                   state[PROVISIONING|ACTIVE|ERROR|REVOKED], version)

SkillDefinition(resource_id, stable_slug,
                current_published_version_asset_id | NONE, status)
SkillVersion(asset_id, skill_resource_id, ordinal,
             artifact_digest, metadata_hash, tool_dependency_resource_ids[],
             state[DRAFT|PUBLISHED|RETIRED])

AgentSession(workspace_id, root_event_id, installation_resource_id,
             agent_version_asset_id, projection_generation,
             runtime_thread_id,
             core_memory_state[FOUND|ABSENT|UNREADABLE],
             core_memory_event_id | NONE,
             status)
AgentInvocation(id, source_event_id, installation_resource_id,
                agent_version_asset_id, projection_generation,
                parent_invocation_id | NONE, delegation_id | NONE,
                workflow_id, reply_event_id | NONE, status)

ToolDefinition(resource_id, name, action_key, input_schema_hash,
               output_schema_hash, backend_ref, status)
ToolBinding(workspace_id, tool_resource_id,
            installation_resource_id | NONE, status)
ToolGatewayProjection(tool_resource_id, gateway_resource_ids[],
                      config_hash, state[PENDING|ACTIVE|ERROR|REVOKED])
```

- `AgentDefinition`、`SkillDefinition`、`AgentInstallation` 和 `ToolDefinition` 都是一对一 Resource 扩展，Tenant、home Workspace、HUMAN owner 和权限只存 Resource；`AgentVersion/SkillVersion` 是对应 Resource 下的一对一 Asset 扩展，仍具有显式 HUMAN owner。
- Agent Definition 是 Tenant 级稳定身份，默认 `home_workspace_id=NONE`；Installation 必须属于同 Tenant 的一个 Workspace。发布的 Version 不可变；修改行为创建新 DRAFT，发布后由 installation 的升级动作显式切换，不自动追随 `current_published_version_asset_id`。
- AgentInstallation `ACTIVE` 还要求其 AgentPrincipal 的 SpiceDB workspace relationship 已投影且 fresh `discover` Check 为真、Buzz Channel roster、ChannelAgentBinding、AgentMemoryBinding 与 AgentRuntimeProjection generation 全部对账。撤任一项先使 Installation 不可用于新 trigger/action，再收敛投影；Agent 不创建 WorkspaceMembership，也不复用 HUMAN membership（DD-50/66）。
- `RuntimeProfile` 是随平台发布的 Catalog 合同，不是用户 Resource。第一期只定义 `SERVER_CODEX`；其源码投影接缝成立，AgentTaskWorkflow 与凭据链按 DD-69/70/71 定义；二者实现并对账前不能标记运行 active。`LOCAL_ACP/REMOTE_PROVIDER` 只保留可识别类型，`web_availability=DISABLED`，不得被 Installation 选择。
- `AgentRuntimeProjection` 是静态运行投影，不是授权权威。其字段来自 `AgentVersion requested ∩ Workspace/Channel/Tool/Skill/LLM bindings ∩ AgentPrincipal permission ∩ Gateway projection ∩ RuntimeProfile capability`；任一层只能收窄，不能放宽。Delegation、Approval、Capacity 与 Quota 属于具体 Invocation/ActionDecision 的动态准入，不写入 projection config hash。
- 固定 reason code 为 `NOT_BOUND / NO_PERMISSION / NO_DELEGATION / WAITING_APPROVAL / QUOTA_BLOCKED / CAPACITY_BLOCKED / PROJECTION_FAILED / RUNTIME_NOT_READY / NOT_AVAILABLE_ON_WEB / NOT_SUPPORTED_BY_RUNTIME / DISABLED`。管理页按字段展示 requested、effective、source 和 reason；不以“配置未生效”笼统代替确定原因。
- SkillVersion 只提供指令和依赖声明。`tool_dependency_resource_ids` 不能创建 ToolBinding、Delegation 或 Resource permission；缺依赖时 skill 的有效状态为 `NOT_BOUND/NO_PERMISSION`。
- 配置升级先生成新的 PENDING projection，验证 Gateway、Skill root 与 runtime 能力后切 ACTIVE。已开始的 Invocation 固定旧 version/generation 并自然结束；新触发只取新 ACTIVE generation。授权撤销不是配置升级，按 `10` 立即阻止后续副作用并终止已知旧连接。
- rollback 与 upgrade 使用同一动作，只把 Installation 指回一个仍可用的旧 PUBLISHED version，并生成新的 projection generation；历史 Invocation/Audit 不改写。
- ToolBinding 与 ChannelAgentBinding 都是 Installation 的受控子配置，继承 Installation Resource 的 HUMAN owner；它们只控制可见性/触发，不授予业务 Resource permission。
- AgentMemoryBinding 是 Installation Resource 的一对一 native child binding，不是新 Resource/Asset。`agent_buzz_identity_binding_id` 必须指向该 Installation 的 AGENT identity，`memory_counterparty_buzz_identity_binding_id` 必须指向同 Tenant 的 CONTROL identity；两者构成唯一 NIP-AE `(agent, counterparty)` namespace。该 counterparty 只是 NIP-AE `p` 加密对手，不是 Resource HUMAN owner；owner 转移不重绑或重加密记忆（DD-66）。
- NIP-AE head 是内容权威。`core_head_event_id/created_at` 只是可对账的最新观测引用，不是 Core 正文副本；冷记忆 listing 只有复合游标完整走到末尾时为 `COMPLETE`，超过显式 10000 条上界为 `BOUND_EXCEEDED`，传输、验签或解密失败为 `UNKNOWN`（SF-BUZ-07/16/19）。若观测 head 超前 Relay 当前时间准入窗口，`head_state=HEAD_AHEAD_OF_RELAY` 并拒绝新写入；不得改写 created_at 或删除 head，恢复只等待时钟窗口重新成立。所有 signer 副本必须使用同步时钟（SF-BUZ-29）。
- AgentSession 必须使用非 ephemeral Codex thread，`runtime_thread_id` 是会话恢复键。创建 Session 时固定当时的 `core_memory_state`与成功读取的 `core_memory_event_id`；活跃 Session 不追随新 core head，新 Session 才取新 head。`UNREADABLE` 时 event ID 为 NONE，不等于 `ABSENT`（DD-67）。
- `memory_policy` 是 immutable AgentVersion 的最大写入边界：HUMAN owner 可通过受治理 Action 管理 core/cold memory；Agent 只在该 Installation 的 active Invocation 中获得内部 core/cold read，只在 `INVOCATION_SCOPED` 时可写 cold memory，只在 `AGENT_WITH_APPROVAL` 且 Temporal Approval 已消费时可写 core。该策略不授予任何业务 Resource permission。
- 唯一 Agent 触发键为 `(tenant_id, source_event_id, installation_resource_id)`。AgentSession/Invocation 固定 exact version 与 projection generation，不能从当前 installation pointer 反推历史配置。
- Core 原生的 Agent/Skill/Installation/Tool Resource 统一指向一个 `class=PLATFORM + platform_port_key=CORE_INTERNAL + ExecutionConnector=INTERNAL` 的 ComponentRelease/Binding；该 binding 只表示 Core 是 native 数据权威，不产生独立服务或额外网络调用。

## 8. Capacity、Quota、Usage、Audit 与 Secret

```text
CapacityLease(id, operation_id, tenant_id, workspace_id | NONE,
              pool_key,
              holder_ref{workflow_id,run_id,activity_id,attempt}, units,
              acquired_at, renewed_at, expires_at,
              state[HELD|RELEASING|RELEASED|EXPIRED|UNKNOWN])

OpenMeterBinding(tenant_id, namespace, customer_id,
                 subject_key_prefix, status, version)

QuotaReservation(id, operation_id, tenant_id, workspace_id | NONE,
                 openmeter_customer_id, meter_key, subject_key,
                 reserved_upper_bound, credit_snapshot_ref,
                 expires_at,
                 state[RESERVED|RELEASED|SETTLEMENT_PENDING|SETTLED|EXPIRED|UNKNOWN])

UsageEvent(id, operation_id, tenant_id, workspace_id | NONE,
           openmeter_customer_id,
           component_binding_ref[kind,id] | NONE, component_release_id | NONE,
           component_projection_generation | NONE, resource_id | NONE,
           agent_installation_resource_id | NONE,
           source_type, source_id, meter_key, subject_key,
           dimensions, quantity, occurred_at,
           settlement_status[PENDING_PUBLISH|ACCEPTED|COMMITTED|FAILED|UNKNOWN],
           openmeter_event_id)

EvidenceRef(authority, evidence_type, stable_id,
            version_or_digest | NONE,
            sensitivity[SUMMARY|RESTRICTED])

AuditEvent(id, event_key, tenant_id | NONE, workspace_id | NONE,
           operation_id, action_execution_id | NONE,
           event_type[AUTHENTICATION|SESSION|INTENT|DECISION|APPROVAL|DISPATCH|OUTCOME|REVOCATION|RECONCILIATION|ACCESS],
           human_identity_id | NONE,
           initiator_principal_id | NONE, actor_principal_id | NONE,
           action_key, action_version,
           component_type_key, component_binding_ref[kind,id] | NONE,
           component_release_id | NONE, component_projection_generation | NONE,
           target_type | NONE, target_id | NONE, parameter_hash,
           decision, result_code, result_exposure,
           evidence_refs[], correlation_id, occurred_at)

OutboxEvent(id, operation_id, type, payload_ref,
            idempotency_key, published_at | NONE)

SecretRef(provider[OPENBAO], locator, version, audience,
          status[PENDING|ACTIVE|SUPERSEDED|REVOKED])
```

- CapacityLease 只用于平台实际控制的 Codex/Wren/CocoIndex runner pool；`capacity_policy=NATIVE` 只保留组件真实 429/health/job 结果，不伪造通用 lease。
- OpenMeter 开源 server 只解析一个固定 default namespace（SF-OMT-06）。全部 Tenant 共用这个 namespace，每个 Tenant 映射唯一 Customer；Workspace/Resource/Agent/operation 使用带 Tenant 前缀、且归属于该 Customer 的 subject key/dimension。OpenMeterBinding 的 `namespace` 必须等于平台固定值，Core 同时校验 `tenant_id→customer_id`，不接受请求参数替换 namespace/customer（DD-38）。
- UsageEvent 的 Tenant、Workspace、Customer、subject、Resource/Agent dimensions 只从 ActionExecution/Resource/OpenMeterBinding 构造；native payload 与 Agent 参数不能覆盖。Core 唯一键是 `(tenant_id, source_type, source_id, meter_key)`；`openmeter_event_id` 在首次 outbox 前由同一组字段确定，重试不得改变。OpenMeter 返回 202 只进入 `ACCEPTED`；按 ID/source 查到事件带 `stored_at` 才 `COMMITTED`。`validation_errors` 与 `customer` 由 OpenMeter 在每次查询时按当时的 meter 集合与 subject→Customer 映射重算，不落库、随配置变化而变化，Core 只把首次读取结果作为配置正确性告警，不作为 `COMMITTED` 的判据。这只确认 metering event 已持久，不等同 invoice finalized；结算侧的稳定结论以 charge/invoice 落库的 `validation_issues` 为准（SF-OMT-07/08、DD-51）。
- STRICT_RESERVATION 只适用于 ActionDefinition 证明最坏上界、且该 Tenant+subject+meter 的全部 usage producer 都经 Core 的路径。Core 对该键串行，把 OpenMeter committed credit snapshot 减去尚未被该快照吸收的 UsageEvent 与 active QuotaReservation 后再创建 reservation（减法项按快照时间点切分，避免重复扣减）；没有外部分流保证、snapshot/query 不可用或上界不成立时不得使用 STRICT。Reservation 不成为 plan/balance/usage 账本（DD-51）。
- CapacityLease 由持有 slot 的 Temporal Activity heartbeat 续租，`renewed_at` 取成功 `RecordHeartbeat` 对应时间，且 Activity `HeartbeatTimeout` 必须小于 lease expiry 间隔。heartbeat 超时只把 lease 置为 `UNKNOWN`，不直接置 `EXPIRED` 或回池；`RELEASED` 只接受对应 Activity 终态作为 holder terminal evidence。Activity retry 的新 `attempt` 更新同一 holder_ref 并复用原 lease，不创建第二份 units。
- `STRICT_RESERVATION` 保存可证明的最坏上界，不保存乐观 estimate。无法给出上界的动作不得使用严格额度准入。
- QuotaReservation 只防止 OpenMeter 202 异步计量期间的并发超卖，不复制 plan、balance 或 committed usage。
- UsageEvent 以 `(tenant_id, source_type, source_id, meter_key)` 唯一；OpenMeter CloudEvent 固定映射为：namespace=`OpenMeterBinding.namespace`，source=`urn:kailo:core:usage`，id=`openmeter_event_id`，subject=`subject_key_prefix + subject_key`，operation_id 只放入 data 的受控 dimension。source 不含 operation，重试保持同一 namespace/source/id/body（SF-OMT-04）。
- AuditEvent 追加写入；`event_key` 对同一 operation/阶段/native attempt 稳定，重试不得复制同一审计事实。它不保存 Secret、正文、完整 prompt/response、原始 SQL、结果行或工具 raw body。
- 所有 Governed Action 的 AuditEvent 必有 `tenant_id`；`workspace_id` 必须与 ActionDefinition 的 WorkspaceRule 解析结果一致。只有 `TENANT_ONLY` 或继承自 NONE 的 child action 才为 NONE，Workspace 内消费 Tenant-level Resource 仍记录实际 Workspace。`tenant_id=NONE` 只允许 `AUTHENTICATION|SESSION` 且发生在 AgentGateway OIDC callback 后、Core 尚未解析可用 TenantMembership 的边界，此时 `human_identity_id` 或不可逆外部 subject hash EvidenceRef 必有一个，Principal/业务 target 必为空（DD-52/54）。
- EvidenceRef 只能保存源码权威中的稳定 ID、version/digest 和敏感级别，不保存原始 payload、临时 URL、cookie、ActionToken、WOPI PAT、Nostr 私钥或 SecretRef locator。BFF 每次 dereference 都以 AuditEvent scope、当前 `audit`/ResultExposure permission 和目标组件 binding fresh授权；原证据已删除或无权时只显示不可用状态，不回退展示 ref 内容。
- `initiator_principal_id` 表示提出业务意图的人或服务，`actor_principal_id` 表示实际执行身份。用户委托 Agent 时两者不同，Delegation/Approval/Tool/Gateway refs 留在 evidence 中，不能只记录 Agent 而丢失用户责任链。
- Business Observability 不另建事实权威表：BFF 以 ActionExecution/TaskProjection、Temporal/native/Gateway/OpenMeter/Audit refs 组成只读 operation view。普通发起者/Resource owner 只看受 ResultExposurePolicy 允许的状态与自身用量；Tenant/Workspace 聚合、错误 evidence 和成本需要目标 `audit` 权限。
- 业务观测的计费原因码固定为 `AVAILABLE` 或 `BILLING_UNAVAILABLE`，usage attribution 固定为确定 operation ref 或 `NONE`。SS-AGW-USAGE 未闭合、durable usage 缺失或 Codex 链的 SS-COD-TRACE 未闭合任一成立时均显示 `BILLING_UNAVAILABLE + NONE`，不创建无法归属的 UsageEvent，也不显示零用量。
- SecretRef 只是外部 secret 版本 locator：`provider=OPENBAO`，`locator=<namespace>/<mount>/<path>`，`version` 为 KV v2 版本号，`audience` 为允许取用的 service identity；Core 从不把 secret 值写入数据库、日志或磁盘。任何 binding 在其全部 SecretRef 指向的版本可读、且该次读取已被 OpenBao audit 记录前不得 active（DD-70）。
- secret 的写入、轮换与撤销是受治理动作，不是运维旁路。Tenant admin 通过 Buzz Web 的 binding 配置界面提交 secret 值，BFF 在同一 Governed Action 内用 Core 的 service token 写入该 Tenant 的 OpenBao namespace 并只把返回的版本号记成 SecretRef；secret 值不进入 Core 数据库、日志、Workflow history 或 AuditEvent，AuditEvent 只记 `(locator, version, audience, 动作, 结果)`。Tenant admin 与任何用户都不直接持有 OpenBao 凭据。
- SecretRef 状态机固定为 `PENDING → ACTIVE → SUPERSEDED → REVOKED`：写入新版本得到 `PENDING`；binding 的新 projection generation 对账通过后该版本转 `ACTIVE`、旧版本转 `SUPERSEDED`；`SUPERSEDED` 版本必须保留到所有固定旧 generation 的在途 ActionExecution/Workflow/ExternalExecution 到达终态之后才允许 `destroy` 并转 `REVOKED`。禁止在旧 generation 仍有在途执行时销毁其 secret 版本——那会让无法重试的执行永久失败。
- Core 自身访问 OpenBao 的引导凭据不由 OpenBao 托管：部署以 response wrapping 一次性投递 AppRole `secret_id`（或在 Kubernetes 部署下改用 Kubernetes auth，由 ServiceAccount token 换取），Core 启动后按 lease 续期；该引导路径与其审计是部署前置条件，不是平台内的 Governed Action。
- 平台权威组件 API 只接受 Core/Worker service identity：SpiceDB 使用 bearer PSK，Temporal 使用 JWT system/namespace role，OpenBao 使用 AppRole/Kubernetes auth 换取的短 TTL service token（DD-70），AgentGateway admin 与 OpenMeter 分别使用 SS-AGW-ADMIN/SS-OMT-AUTH 的 Core-only 接缝。native credential 只认证服务，不把其角色当作 HUMAN/AGENT 业务授权；每个用户可达动作仍以 ExecutionContext 做 SpiceDB Check（DD-37）。
