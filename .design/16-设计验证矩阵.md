# 设计验证矩阵

本矩阵只验证输入、权威判断、状态与禁止结果，不是实施或测试计划。状态严格使用 README 的四类状态。

## 1. 产品要求闭合

| ID | 要求 | 设计裁决 | 状态 |
|---|---|---|---|
| V-REQ-01 | Buzz Client Surface 三端是仅有入口 | Web 与 Desktop 共用 TypeScript 主体：静态 routeTree+BFF，一次性 Component Host 提供固定 catch-all 与运行时 module；Mobile 为独立 Dart 主体、非组件宿主。浏览器本地 Agent 与本机自动化仍不进拓扑（DD-74、SF-DSK-01、SF-MOB-01/02、SS-WEB-01、SS-WEB-COMPONENT-HOST、SF-COD-03/04） | `ADAPTER_REQUIRED: SS-WEB-01、SS-WEB-COMPONENT-HOST` |
| V-REQ-02 | Tenant/Workspace 多人多 Agent 协作 | Tenant→Community、Workspace→Channel；独立 HUMAN/AGENT Principal/BuzzIdentity（SF-BUZ-01/03/07） | `DESIGN_DEFINED` |
| V-REQ-03 | 数据面与管理面统一在 Client Surface | Channels/Tasks/Approvals 与 Drive/Knowledge/Data/Agents/Governance 均为内置模块 | `DESIGN_DEFINED` |
| V-REQ-04 | 组件松耦配置接入且兼容 release 无需重发主体 | Definition→immutable Release/Package；Platform 使用 PlatformProviderBinding，Application 使用 ApplicationBinding，两者都产生 RuntimeProjection；一次性 Host/Adapter Protocol 后只加载平台批准、digest/API/slot 兼容的 release（SF-MAT-01/03/04、SS-WEB-COMPONENT-HOST、`07`；SF-MAT-06 为反例） | `ADAPTER_REQUIRED: SS-WEB-COMPONENT-HOST` |
| V-REQ-05 | 新资源自动进入授权 | Resource+active HUMAN owner+SpiceDB relationships+native scope 对账后 active（SF-SPZ-01/02） | `DESIGN_DEFINED` |
| V-REQ-06 | Agent 委托、确认与审批 | Agent∩grantor∩Delegation∩Binding∩Exposure；Codex pause→Temporal Approval | `DESIGN_DEFINED`（GAP-TMP-01 已闭合，DD-69）；PEP=`ADAPTER_REQUIRED: SS-AGW-PEP` |
| V-REQ-07 | Approval/Workflow/任务工作台统一 Temporal | Server lifecycle 存在，Application Worker 以 `temporal-sdk-go` 自建（SF-TMP-01/02/04、SF-TSDK-02/03/07） | `DESIGN_DEFINED`（GAP-TMP-01 已闭合，SF-TSDK-01/02/03/07/08，DD-69） |
| V-REQ-08 | 统一认证贯穿 | Gateway OIDC→BFF→Principal；Core/Worker→SpiceDB/Temporal native auth；Gateway admin/OpenMeter闭合Core-only auth；secret缺失 | `ADAPTER_REQUIRED: SS-AGW-OIDC/SS-AGW-ADMIN/SS-OMT-AUTH`（GAP-SEC-01 已闭合，DD-70） |
| V-REQ-09 | Capacity/Quota/Billing | Core CapacityLease/QuotaReservation+OpenMeter；OpenMeter auth与Gateway durable usage必须闭合 | `ADAPTER_REQUIRED: SS-OMT-AUTH/SS-AGW-USAGE` |
| V-REQ-10 | Operation Audit/Business Observability | 服务端 Action/Audit 与各权威 native refs 联合读模型，不复制正文/账本 | `DESIGN_DEFINED` |
| V-REQ-11 | 所有 Resource/Asset 有 HUMAN owner | Core owner事实+SpiceDB owner投影；Agent/service仅 producer | `DESIGN_DEFINED` |
| V-REQ-12 | 各类资源真实增量更新 | native/versioned/stream各归原权威；跨 Resource 用稳定 LMDB CocoIndex binding | `ADAPTER_REQUIRED: SS-CEL-MATERIALIZATION、SS-WEK-MATERIALIZATION`（Cells→WeKnora 按 hash 归并后 create-new→ready→delete-old；GAP-SEC-01/GAP-TMP-01 已闭合，DD-69/70） |
| V-REQ-13 | 标准远程模型调用统一 Gateway | Codex/WeKnora→AgentGateway；Codex/WeKnora trace与 durable usage需精确适配 | `ADAPTER_REQUIRED: SS-COD-TRACE、SS-WEK-ASYNC-TRACE、SS-WEK-LLM-CTX、SS-AGW-USAGE` |
| V-REQ-14 | 知识图谱不建第二套 | WeKnora graph active；EdgeQuake excluded（SF-WEK-05、SF-EDG-01） | `DESIGN_DEFINED` |
| V-REQ-15 | Agent 管理服从全局治理 | Core Definition/Version/Installation；Buzz Desktop 的确定语义接缝为 SS-BUZ-AGENT-MODEL | `ADAPTER_REQUIRED: SS-BUZ-AGENT-MODEL` |
| V-REQ-16 | Cells 文件在 Buzz 中预览/编辑 | 原生 WOPI主链+Platform Human/session/fresh PEP+Cells/DocumentServer精确接缝（SF-CEL-05..11、SF-OFF-03/08） | `ADAPTER_REQUIRED: SS-CEL-WOPI、SS-OFF-WOPI` |
| V-REQ-17 | 主题+i18n贯穿全部前端 | Buzz ThemeProvider/AppLocale；ONLYOFFICE launch映射（SF-WEB-03、SF-OFF-07） | `ADAPTER_REQUIRED: SS-WEB-PRESENTATION` |
| V-REQ-18 | 禁止跨 Tenant 直接共享，只允许两段导出/导入 | 同 Tenant relationship 不变式；源 ExportPackage Asset→HUMAN download→目标 session upload→新对象/新 owner（DD-42） | `DESIGN_DEFINED`（GAP-TMP-01 已闭合，DD-42/69） |
| V-REQ-19 | Tenant 删除、Workspace 撤权、组件停用可审计收敛 | Membership/Component disable 的 ComponentTaskWorkflow；Tenant snapshot/owner approvals/ExternalExecution/不可逆边界（DD-41/43/44） | `BLOCKED: GAP-LCM-01`（GAP-TMP-01 已闭合；撤权/停用=`DESIGN_DEFINED`，DD-69） |
| V-REQ-20 | Agent 会话/长期记忆分层且只有一个长期权威 | Relay 协作历史 + Codex durable thread + Buzz NIP-AE core/cold + WeKnora knowledge；Codex MemoryTool/OpenViking excluded（SF-BUZ-16/17/20、SF-COD-09/10、DD-65） | `ADAPTER_REQUIRED: SS-BUZ-ENGRAM`（GAP-SEC-01/GAP-TMP-01 已闭合，DD-69/72） |
| V-REQ-21 | 三端能力分级不改变治理结论 | Web/Desktop 承载 Component Host 与 editor surface；Mobile 不加载 remote module、不承载 editor surface，也不以内嵌 WebView 替代；缺失入口时该能力在该端不可见而非降级绕过。三端对同一 Governed Action 的准入、审批、额度与审计结论相同（DD-74/75） | `DESIGN_DEFINED` |
| V-REQ-22 | 第三方可独立完成组件接入 | 六件接入面（SDK、manifest 校验器、脚手架、本地开发环境、一致性套件、示例组件）随 Component Host 同 Stage 交付；验收判据是未参与平台开发的工程师仅凭接入面完成一个组件并通过一致性套件（DD-76） | `DESIGN_DEFINED` |

### 1.1 V-REQ 到 `01` 需求编号的映射

`16` 的 V-REQ 是验证用的连续编号，不是 `01` 的需求 ID；追溯以本表为准。

| V-REQ | `01` 来源 |
|---|---|
| 01、02、03 | REQ-01、REQ-02 |
| 04 | REQ-11 |
| 05、06、07、08、09、11 | REQ-03 |
| 10 | REQ-03、REQ-06 |
| 12 | REQ-04 |
| 13 | REQ-05 |
| 14 | REQ-04 与 §5 一期排除（EdgeQuake） |
| 15 | REQ-03 与 §2 一期范围（Agent 管理内置） |
| 16 | REQ-07 |
| 17 | REQ-08 |
| 18 | REQ-09 |
| 19 | REQ-10 |
| 20 | REQ-20 |

## 2. 关键交互

| ID | 输入 | 必须成立 | 必须禁止 | 状态 |
|---|---|---|---|---|
| V-SCN-01 | OIDC Human发 Channel消息 | Gateway OIDC→BFF typed command→membership/admission→Human Nostr代签→Relay signer guard | Browser key/direct publish/raw event；共享 service signer | `ADAPTER_REQUIRED: SS-AGW-OIDC`（GAP-SEC-01 已闭合，私钥托管 DD-72） |
| V-SCN-02 | logout | Gateway 原生同源 POST 清本地 cookie；SS-AGW-OIDC 同步撤 Core PlatformSession、关 stream；下次重认证 | 宣称 IdP 全局 logout/revoke | 本地 cookie=`UPSTREAM_SUPPORTED`；平台协同=`ADAPTER_REQUIRED: SS-AGW-OIDC`；`09` §5 的注销合同 |
| V-SCN-03 | Tenant/Workspace 建立或撤销 | Core→ComponentTaskWorkflow→SpiceDB→Buzz projection→query reconcile；撤销先 fail closed | 未对账开放 scope；Buzz失败恢复权限；脱离 Temporal 对账 | `DESIGN_DEFINED`（GAP-TMP-01 已闭合，DD-69） |
| V-SCN-04 | Relay 断线后恢复 Agent | persisted query+checkpoint+稳定触发键 | ACP内存 queue作为唯一恢复源 | `DESIGN_DEFINED` |
| V-SCN-05 | Agent仅有 `consume` | answer/citation/aggregate/受控 derived Asset | raw file/chunk/SQL row/model export；Approval补 permission | `DESIGN_DEFINED`；`03` §5 的 `consume` 规则 |
| V-SCN-06 | 高风险 MCP | Codex暂停→Temporal Approval→fresh admission→ExtMcp PEP | 批准前 backend call；Gateway等待审批 | `ADAPTER_REQUIRED: SS-AGW-PEP`（GAP-TMP-01 已闭合，DD-69） |
| V-SCN-07 | 批准后撤权 | fresh Check拒绝，旧连接按已知边界关闭 | 旧 approval/token继续副作用 | `DESIGN_DEFINED` |
| V-SCN-08 | native task取消 | 按 Cells/WeKnora真实 capability发控制并继续 observe | control accepted显示已取消 | `UPSTREAM_SUPPORTED` |
| V-SCN-09 | Wren长查询取消 | 停止等待、屏蔽迟到结果、capability=UNSUPPORTED | kill共享runtime；显示数据库已取消 | `DESIGN_DEFINED` |
| V-SCN-10 | Wren执行 SQL | 固定 SemanticModel runtime、strict=true、数据库只读 credential、行限 | 切换任意project/profile；strict替代DB只读角色 | `ADAPTER_REQUIRED: SS-AGW-PEP`（GAP-SEC-01 已闭合；数据库只读 credential 按 DD-70/71 托管与投递） |
| V-SCN-11 | Codex尝试shell/process/apply_patch | CODEX_HOME 中无 local environment 是跨 resume 的承重封堵；`thread/start.environments=[]`（client `initialize` 已声明 `experimentalApi`）且 Core 不发 turn override 是纵深防御；模型 exec/apply_patch/view_image 均不注册，三个 host RPC 因无 environment 拒绝且 Core 不调用 | 以关闭 `unified_exec`、`shell_type` 或只传 thread 空数组当承重封堵；host shell/process/local automation | `DESIGN_DEFINED`（DD-15） |
| V-SCN-12 | AgentGateway Catalog更新 | Core通过SS-AGW-ADMIN认证后写ConfigResource并回读hash | xDS反向写；Browser/Agent直访；仅靠内网/CORS | `ADAPTER_REQUIRED: SS-AGW-ADMIN` |
| V-SCN-13 | Gateway OIDC identity进入BFF | OIDC先执行，transformation移除外来header再写validated subject | BFF信任Browser自报header或IdP role | `ADAPTER_REQUIRED: SS-AGW-OIDC` |
| V-SCN-14 | OpenMeter重试 | 同namespace/source/id/body，ingest+sink dedupe开启 | 新event ID；依赖ledger group去重 | `DESIGN_DEFINED` |
| V-SCN-15 | strict quota并发 | 可证明上界+原子reservation | 乐观estimate；第二余额账本 | `DESIGN_DEFINED` |
| V-SCN-16 | Gateway log写库失败 | 业务观测显示缺口；不创建billable usage | 用游标/Codex/WeKnora自报补洞 | `ADAPTER_REQUIRED: SS-AGW-USAGE` |
| V-SCN-17 | durable usage重放 | 稳定log ID→唯一UsageEvent→OpenMeter committed | 按读取次数重复计费 | `ADAPTER_REQUIRED: SS-AGW-USAGE` |
| V-SCN-18 | 新组件 release 注册 | Platform Catalog Tenant/platform admin；固定 source commit、manifest/Host/Adapter Protocol range、ComponentPackage/Web artifact digests、Action/Tool、全局治理、observability 与 presentation；Temporal 审批后 APPROVED | Tenant admin 上传代码；package 携带后端 executable；route 自动成 Tool；动态 WriteSchema；缺合同仍批准 | `DESIGN_DEFINED`（GAP-TMP-01 已闭合，DD-69）；Host=`ADAPTER_REQUIRED: SS-WEB-COMPONENT-HOST` |
| V-SCN-19 | Cells新文件进入KB | 完整snapshot→stable UUID/revision→Coco diff→WeKnora ready→usage/audit | 全量重建；partial snapshot删除 | `ADAPTER_REQUIRED: SS-CEL-MATERIALIZATION、SS-WEK-MATERIALIZATION`（GAP-SEC-01/GAP-TMP-01 已闭合，DD-69/70） |
| V-SCN-20 | CocoIndex runner迁移 | 解析同一 `coco_environment_ref/coco_app_name` 后运行 | state不可达时换空目录继续 | `DESIGN_DEFINED`；`03` §3 MaterializationBinding 不变式与 DD-32 |
| V-SCN-21 | Cells同node正文改变 | 重算 hash 引用集合；create-new→ready→旧 hash 无引用后 delete-old；失败保留old/pending | 对共享 Knowledge 原位替换；宣称跨系统原子 | `ADAPTER_REQUIRED: SS-CEL-MATERIALIZATION、SS-WEK-MATERIALIZATION` |
| V-SCN-22 | WeKnora graph consume | 图谱摄取只在 `NEO4J_ENABLE`、Neo4j 连接、`IndexingStrategy.GraphEnabled` 与 `ExtractConfig.Enabled` 均成立时生成 subtasks；chat pipeline 再以 `NEO4J_ENABLE`+`ExtractConfig.Enabled` 筛选已有图数据的检索范围。`query_knowledge_graph` 是同 KB 的 consume tool，返回 `HybridSearch` 结果，不因名称获得额外权限或图谱访问 | 把该工具当作 Neo4j 直连；自动建立 EdgeQuake 第二图 | `DESIGN_DEFINED` |
| V-SCN-23 | Workspace安装/升级Agent | exact immutable version、独立Principal/pubkey/runtime、new generation对账后切换 | Buzz Desktop store当服务端权威；热改运行中turn | `ADAPTER_REQUIRED: SS-BUZ-AGENT-MODEL、SS-COD-RUNTIME` |
| V-SCN-24 | Office打开/保存 | Core service+DocumentSession解析Platform Human/admitted mode→PAT；fresh Check→WOPI；PutFile evidence+NodeVersions才SAVED | 把Core service claims当editor user；按readonly直接发rw；普通callback桥；历史revision换latest | `ADAPTER_REQUIRED: SS-CEL-WOPI` |
| V-SCN-25 | Office编辑撤权 | 停新launch、撤PAT、Check/Get/Put fresh reject | 旧session继续保存；删除历史evidence | `ADAPTER_REQUIRED: SS-CEL-WOPI` |
| V-SCN-26 | 主题/locale切换 | Buzz native实时；新WOPI launch映射；dirty editor不reload | 组件独立主题；SYSTEM作为第三editor theme | `DESIGN_DEFINED` |
| V-SCN-27 | OpenMeter Tenant归属 | 固定default namespace；Tenant→唯一Customer；subject归该Customer；Core-only auth | per-request namespace；调用方自报customer/subject；无认证直连 | `ADAPTER_REQUIRED: SS-OMT-AUTH` |
| V-SCN-28 | Browser 读聊天/presence/inbox/media | 所有 query/subscribe/NIP-98/media 经 SS-WEB-RELAY BFF transport，stream 按 active scope 过滤 | Browser获得Relay URL/signer；任意filter或直连media endpoint | `ADAPTER_REQUIRED: SS-WEB-RELAY` |
| V-SCN-29 | 收藏/静音/已读跨端同步 | Core CollaborationUserState按session principal/scope读写 | 服务端解密kind:30078；Browser localStorage当权威 | `DESIGN_DEFINED` |
| V-SCN-30 | HUMAN 把 Resource 转入另一 Tenant | 源 export 和目标 import 分属两 session/operation/Workflow；format+digest 重验；目标新 owner/ACL/native ID | 跨Tenant relationship/binding/context；继承源owner/ACL/token；无transfer format仍显示Action | `DESIGN_DEFINED`（GAP-TMP-01 已闭合，DD-42/69） |
| V-SCN-31 | Tenant suspend/restore/delete | suspend先fail closed并冻结snapshot；restore只在未删native时对账开放；delete要求admin+所有distinct owner批准与native terminal | 在当前门禁下暴露delete Action；删除dispatch后声称数据可还原；有retained/unknown ref仍标DELETED；删audit/usage | suspend/restore=`DESIGN_DEFINED`（GAP-TMP-01 已闭合）；delete=`BLOCKED: GAP-LCM-01` |
| V-SCN-32 | Workspace member revoke/regrant | Core membership先REVOKING/fail closed→MembershipRevocation Workflow→SpiceDB/Buzz撤销→对账REVOKED；regrant新version重建 | 从roster反推Core成员；投影失败继续allow；复活旧relationship缓存；脱离 Temporal 对账 | `DESIGN_DEFINED`（GAP-TMP-01 已闭合，DD-41/69） |
| V-SCN-33 | Platform/Application Binding disable | Core 先 DISABLING/fail closed→ComponentDisable Workflow（固定 binding kind/ID/version）→Schedule/relationship/route/tool/connection/native refs/usage/audit 对账 | 单次改 status 即报完成；将 disable 写成 native delete；丢 retained ref | `DESIGN_DEFINED`（GAP-TMP-01 已闭合，DD-44/69） |
| V-SCN-34 | Tenant member revoke/regrant | Core TenantMembership 先REVOKING，撤session/全Workspace准入→owner转移→MembershipRevocation Workflow撤SpiceDB tenant/workspace relationships与Buzz relay/Channel roster→对账REVOKED；regrant新version | 为等owner转移继续允许用户动作；有active owner引用即REVOKED；从Buzz roster反推Core成员；删历史Principal/Audit | `DESIGN_DEFINED`（GAP-TMP-01 已闭合，DD-45/69） |
| V-SCN-35 | Workspace owner成员被撤权 | WorkspaceMembership `REVOKING`后scope guard立即拒绝；直接Resource owner关系不能绕过；SpiceDB/Channel roster收敛后`REVOKED` | 只依赖Resource owner Check继续放行；等待Buzz投影才封禁 | `DESIGN_DEFINED`（GAP-TMP-01 已闭合，DD-41/69） |
| V-SCN-36 | Workspace暂停/恢复/删除入口 | 暂停先fail closed并archive Channel，保留Resource/Asset/owner；恢复完整对账后开放；无delete Action | 使用DELETE_GROUP暂停；软删Channel后宣称Workspace及业务资产已删除；保留不可达删除状态 | `DESIGN_DEFINED`（GAP-TMP-01 已闭合，DD-69）；delete=`EXCLUDED` |
| V-SCN-37 | 多Owner+Tenant admin审批 | Workflow input冻结全部owner ID/version和角色要求；每个不同owner及admin组分别满足；逐人决定不可变；任一有效DENY终结 | 用单一`min_approvals`凑人数；Agent/SERVICE批准；Core直接改审批状态；复用已CONSUMED批准 | `DESIGN_DEFINED`（GAP-TMP-01 已闭合，DD-69；Validator 只拦确定性状态错误，权限检查在 handler 体内的 Activity，SF-TSDK-09） |
| V-SCN-38 | Temporal Start/native dispatch响应不明 | 预存固定WorkflowRef并Describe/History；预存scoped ExternalExecution/idempotency key，按登记query seam对账 | 分配新workflow ID重跑；native ID为空就重放副作用；信任callback自报scope | `DESIGN_DEFINED`（GAP-TMP-01 已闭合，DD-48/69） |
| V-SCN-39 | Agent委托、计次与ActionToken | action+target/创建Workspace封闭scope；首次dispatch按operation唯一计次；token精确audience/参数hash/最小ZedToken；PEP fresh Check | `NONE`当全局通配；retry重复扣次；只验token签名；token allow覆盖撤权 | `ADAPTER_REQUIRED: SS-AGW-PEP`；credential=`DESIGN_DEFINED`（GAP-SEC-01 已闭合，DD-70/71） |
| V-SCN-40 | Agent进入Workspace | Workspace ACTIVE + Installation/Principal + workspace discover + Buzz roster + ChannelAgentBinding均active，再查目标permission/Delegation | 给Agent伪造HUMAN WorkspaceMembership；仅凭频道event、ToolBinding或Resource permission进入 | `DESIGN_DEFINED`（GAP-TMP-01/GAP-SEC-01 已闭合） |
| V-SCN-41 | 严格Quota并发准入 | 仅封闭producer+可证上界；按Tenant+subject+meter串行合并committed/unsettled/reserved；202仅ACCEPTED，查证后COMMITTED | 宣称OpenMeter原生reserve/分布式原子扣减；忽略未结算usage；外部producer仍承诺hard limit | `ADAPTER_REQUIRED: SS-OMT-AUTH`；LLM另受SS-AGW-USAGE |
| V-SCN-42 | CapacityLease heartbeat超时且holder未知 | 停续租→UNKNOWN→查询/终止holder→Activity terminal evidence成立才释放units | 到期立即复用slot并把仍运行任务当终止 | `DESIGN_DEFINED`；`03` §8 CapacityLease 规则、SF-TSDK-04、DD-69 |
| V-SCN-43 | pre-Tenant认证审计与evidence查看 | 仅AUTHENTICATION/SESSION允许tenant NONE，HumanIdentity/subject hash归因且无业务target；EvidenceRef按当前权限解析 | 业务Action无Tenant；保存raw subject/payload/token/临时URL；拿ref绕过授权 | `ADAPTER_REQUIRED: SS-AGW-OIDC` |
| V-SCN-44 | 用量事件与账单终态 | 202→ACCEPTED；event `stored_at`→COMMITTED；`validation_errors` 只作读时配置告警；结算侧读 charge/invoice `validation_issues`；invoice finalized单独展示 | 202或outbox published即称用量已提交/账单已结算；以读时重算的 validation_errors 为空作终态 | `ADAPTER_REQUIRED: SS-OMT-AUTH` |
| V-SCN-45 | 全壳层主题与i18n | React根均在ThemeProvider；平台文案走MessageKey/t，日期数字走getLocale；用户内容原文；OnlyOffice按launch映射 | 宣称跨设备主题/可切换locale；翻译用户正文；显示native英文为状态文案；嵌入组件原生UI | `ADAPTER_REQUIRED: SS-WEB-PRESENTATION`；OnlyOffice=`UPSTREAM_SUPPORTED`映射 |
| V-SCN-46 | Workspace Agent调用Tenant级LLM Route | 父Invocation Workspace通过guard；child action INHERIT_PARENT；target home仍NONE；usage/audit保留Workspace | 因Resource home NONE把operation Workspace清空；由模型名推断scope；把Tenant管理Action放入Workspace | `ADAPTER_REQUIRED: SS-AGW-PEP` |
| V-SCN-47 | Tenant admin 启用已批准 Application Component | ApplicationBinding pin exact release；配置按 release schema 规范化并固定 digest，secret 只存 SecretRef；native scope 与 projection 完整对账后 ACTIVE | 修改 release/digest；secret 混入普通 config；使用 latest；native scope 失败后回退默认实例；release 批准替代 Resource owner 批准 | `DESIGN_DEFINED`（GAP-TMP-01/GAP-SEC-01 已闭合，DD-69/70） |
| V-SCN-48 | 承载端（Web/Desktop）加载 remote module | BFF 返回 active+authorized 完整 snapshot；session stream generation 变化只触发 refetch/reconcile；Host 建内部 pending load，module 同步 register 暂存 descriptor，script load 后精确校验 type/release/generation/digest/API 并提交；module 仅登记固定 slot 并取得窄 Host API | 以事件当完整状态；Browser 加载外域 script/放宽 CSP；无 pending 注册；把会话 ticket 交给静态 artifact；Tenant 上传 JS；传完整 store/credential | `ADAPTER_REQUIRED: SS-WEB-COMPONENT-HOST` |
| V-SCN-49 | Cells/WeKnora/Wren等非分离组件接入 | 保持native monolith/service；独立remote adapter解析binding/scope；前端用HOST_NATIVE或批准module | 强拆原项目；Core启动上传executable；嵌原生管理UI绕过治理 | 组件合同=`DESIGN_DEFINED`；Cells/WeKnora adapter=`ADAPTER_REQUIRED: SS-CEL-MATERIALIZATION、SS-CEL-WOPI、SS-WEK-MATERIALIZATION`；Wren PEP=`ADAPTER_REQUIRED: SS-AGW-PEP` |
| V-SCN-50 | Component release升级/回滚 | old/new release与generation固定；PENDING对账后原子切新流量；已开始执行固定旧generation | 原地改release；自动追latest；改写历史execution；新generation失败仍切换 | `DESIGN_DEFINED`（GAP-TMP-01 已闭合，DD-69） |
| V-SCN-51 | 新release要求新permission/port/Workflow/slot | 判定超出兼容边界，拒绝运行时注册并保持旧binding | manifest动态扩展SpiceDB schema、平台权威或Host API；声称无需主体发布 | `DESIGN_DEFINED` |
| V-SCN-52 | Component release被撤销 | 禁止新binding/upgrade；既有binding先fail closed再走COMPONENT_DISABLE，保留execution/usage/audit | 静默切latest；删除历史；继续开放新Action | `DESIGN_DEFINED`（GAP-TMP-01 已闭合，DD-44/69） |
| V-SCN-53 | 为已有 platform port 更换远程 provider | PlatformProviderBinding pin exact PLATFORM release；PENDING generation 通过 protocol/service identity/config/secret/readiness/governance 校验后只切新调用；旧调用保持旧 generation | 为 provider 伪造业务 Tenant/Resource；未对账切流；强制改写运行中 Workflow | `DESIGN_DEFINED`（Platform Port Router 为平台自有模块；GAP-TMP-01 已闭合，DD-69） |
| V-SCN-54 | Platform manifest 声明新 port | Core 拒绝运行时注册并保留现有 provider | manifest 自动创建新权威端口 | `DESIGN_DEFINED` |
| V-SCN-55 | 组件通知丢失或 Browser 恢复可见 | 重取 scope-authorized 完整 snapshot，以 generation 做 load/dispose reconciliation | 永久依赖增量事件；保留已失权 module/menu | `ADAPTER_REQUIRED: SS-WEB-COMPONENT-HOST` |
| V-SCN-56 | 非分离 monolith 作为 Application Component | 原服务独立运行；adapter 通过已证明 native API 实现 handshake/scope/execute/observe/cancel/reconcile；Buzz 使用 Host-native 或 approved module | 强拆上游；package 启动 executable；嵌原生管理 UI 绕过治理 | 接入合同=`DESIGN_DEFINED`；每个具体 native adapter 的能力状态由对应组件行及其具名 SS 裁决 |
| V-SCN-57 | 新 embedded driver release 运行时注册 | 只有当前 Core/Worker registry 已有 exact driver key/code 时允许建 binding；新代码要求主体发布 | 上传 executable/动态库；manifest 声明未编译 driver 即加载 | `DESIGN_DEFINED`；新代码=`CORE/WORKER RELEASE REQUIRED` |
| V-SCN-58 | 新 AgentSession 启动 | 固定 Installation/Version/projection；读一次 NIP-AE core 并固定 event ID；创建非 ephemeral Codex thread | 用 Buzz ACP 内存 history；全量注入 cold memory；把 memory 当 system policy；活跃 Session 静默换 core | `ADAPTER_REQUIRED: SS-BUZ-ENGRAM`（GAP-SEC-01 已闭合，DD-72） |
| V-SCN-59 | core 读取返回空/错误 | 只有空集合为 ABSENT；transport/验签/解密/解析/超时为 UNREADABLE；两者不阻塞 Session | 把 UNREADABLE 当无记忆；注入 onboarding 并覆盖真实 core | `UPSTREAM_SUPPORTED` 语义；服务端=`ADAPTER_REQUIRED: SS-BUZ-ENGRAM` |
| V-SCN-60 | Agent 写 cold memory/core | 只作用自身 Installation pair；cold 要求 INVOCATION_SCOPED；core 要求 AGENT_WITH_APPROVAL+Temporal Approval；publish 后重读 head；usage/audit 不存正文 | memory 扩权；Agent 自报 pubkey/counterparty/Relay；冲突后静默覆盖；Agent 自批 | `ADAPTER_REQUIRED: SS-BUZ-ENGRAM`（GAP-SEC-01/GAP-TMP-01 已闭合，DD-68/69/72） |
| V-SCN-61 | Codex runtime 重启/读取历史 | 以 AgentSession `runtime_thread_id` 调 `thread/resume`，以 paginated API 读 turn/item；Relay 只是协作/触发对账权威 | 从 Relay 重放合成新 rollout；创建新 thread 并丢失原会话；启用 Codex MemoryTool 补洞 | `ADAPTER_REQUIRED: SS-COD-RUNTIME`；Codex store=`UPSTREAM_SUPPORTED` |
| V-SCN-62 | Gateway reverse-proxy凭据头卫生 | 所有 Kailo route 在 upstream dispatch 前无条件删除外来 `Proxy-Authorization`，且不使用 permissive authentication | 普通 reverse-proxy route 继承 forward-proxy 的保留行为并把代理凭据送到 upstream | `DESIGN_DEFINED`（DD-73） |
| V-SCN-63 | 原生端本地签名发布协作消息 | Desktop/Mobile 以本机私钥签名后直连 Relay；Relay 按 signer/scope/membership/kind 顺序校验并持久（SF-DSK-01、SF-MOB-01、SF-BUZ-03/07）；Core 不在该路径上，不产生发布前 admission 记录，消息经 Relay 投影后进入审计 | 声称 Core 对原生端消息做过发布前准入；或以持有私钥为由放宽管理平面准入 | `DESIGN_DEFINED`（DD-75） |
| V-SCN-64 | 原生端成员被撤权 | Core 撤 SpiceDB relationship 与 Buzz relay/Channel roster 并关闭已知连接；Relay 随即拒绝该 pubkey 的 event 与订阅，与私钥在客户端无关（`10` §4、SF-BUZ-26） | 依赖平台停止签名来收敛；或在 `require_relay_membership=false` 下声称撤权成立 | `DESIGN_DEFINED`（DD-75） |
| V-SCN-65 | Mobile 打开需要组件或 Office 编辑的能力 | 该入口在 Mobile 不渲染；若由深链接进入则返回稳定 reason code 并提示在 Web/Desktop 继续，不加载任何 remote module、不打开 editor iframe | 以内嵌 WebView 承载组件或编辑器；或渲染入口后在点击时静默失败 | `DESIGN_DEFINED`（REQ-21、DD-74） |
| V-SCN-66 | 第三方仅凭接入面完成组件接入 | 用脚手架生成组件、在本地开发环境跑通、一致性套件全过、提交 release 后由平台管理员批准并 binding 启用；全过程不读 `.design` 全文、不接触平台源码 | 需要阅读平台源码或设计全文才能实现；或一致性套件通过但真实平台拒绝注册 | `DESIGN_DEFINED`（REQ-22、DD-76） |
| V-SCN-67 | Tenant 成员邀请与兑换 | Tenant admin 签发一次性邀请（只存摘要、明文只回一次）→ 已认证 Human 兑换 → membership `INVITED` 与 `tenant.member.admit` 审批 → Tenant admin 确认 → 完整重新准入 → `MEMBERSHIP_PROJECTION` → `ACTIVE`；过期、撤回、重复或并发兑换、否决、邀请人失权都有确定终态并留审计 | 凭据明文入库、日志或 history；兑换即开通而无 admin 确认；从 IdP claim 或邮箱推导成员；过期或撤回的凭据仍可兑换；同一凭据兑换两次 | `DESIGN_DEFINED`（DD-83） |

## 3. 源码边界裁决

| ID | 问题 | 固定结论 | 证据类型 |
|---|---|---|---|
| V-SRC-01 | Buzz原生 Team/Tenant/OIDC | 无；有Community/Channel与Nostr auth；NIP-FI 仅为 `buzz-auth` 库，relay 无调用点（SF-BUZ-22） | `SOURCE_FACT` |
| V-SRC-02 | AgentGateway有Browser OIDC | 有login/session/claims/local logout；不保存access/refresh token，不调用IdP end-session/revoke | `SOURCE_FACT` |
| V-SRC-03 | Temporal能否不要Worker | 不能；Server调度，Worker Poll/Respond | `SOURCE_FACT` |
| V-SRC-04 | Nexus External能否替代Worker | 不能；当前External target被拒绝 | `SOURCE_FACT` |
| V-SRC-05 | AgentGateway xDS是否写配置面 | 否；它是外部ADS client | `SOURCE_FACT` |
| V-SRC-06 | Gateway request log是否完整计费源 | 否；可选DB且写失败后清批次 | `SOURCE_FACT` |
| V-SRC-07 | Wren运行形态 | Python FastMCP；每进程固定project/engine；支持Streamable HTTP | `SOURCE_FACT` |
| V-SRC-08 | Wren strict是否最终写保护 | 否；默认false且planned SQL parse error fail open | `SOURCE_FACT` |
| V-SRC-09 | Wren通用query cancel | 无 | `SOURCE_FACT` |
| V-SRC-10 | CocoIndex状态位置 | 本地LMDB，`db_path` 可选、缺省静默回落相对路径 `./coco_state`；App name 独立 named sub-database | `SOURCE_FACT` |
| V-SRC-11 | CocoIndex是否独立scheduler/API | 否；是SDK runtime | `SOURCE_FACT` |
| V-SRC-12 | Buzz Agent管理是否服务端能力 | 否；关键store/effective/restart位于Desktop Tauri/UI | `SOURCE_FACT` |
| V-SRC-13 | WeKnora是否有实体关系图谱 | 有完整extraction→Neo4j→retrieval/tool链 | `SOURCE_FACT` |
| V-SRC-14 | OpenMeter是否有action reserve与默认去重 | 无reserve；ingest/sink dedupe默认关闭 | `SOURCE_FACT` |
| V-SRC-15 | Cells/ONLYOFFICE原生WOPI是否相交 | 相交；copy/download/timestamp/fixed revision仍需精确适配 | `SOURCE_FACT` |
| V-SRC-16 | ONLYOFFICE OS是否固定20人gate | 否；存在独立memory guard | `SOURCE_FACT` |
| V-SRC-17 | OpenMeter OSS是否动态Tenant namespace且API已认证 | 否；static default namespace，v3 authentication no-op | `SOURCE_FACT` |
| V-SRC-18 | Cells document PAT是否投影Platform session/user/permission | 否；ClientID未使用，user取Cells claims，rw只看readonly | `SOURCE_FACT` |
| V-SRC-19 | AgentGateway admin是否有认证middleware | 否；UI/debug/shutdown同一admin router，仅有CORS | `SOURCE_FACT` |
| V-SRC-20 | Temporal/SpiceDB是否有service authentication | 有；Temporal须显式选default JWT mapper/authorizer且空配置为no-op，SpiceDB强制bearer PSK；均不表达Kailo最终用户权限 | `SOURCE_FACT` |
| V-SRC-21 | Buzz Web是否只需替换发消息 signer（仅 Web 端；原生端本地持钥直连，见 DD-75） | 否；Relay client还承载query/subscribe/NIP-42/NIP-98、media、Agent/inbox与NIP-44 user-state，完整边界为SS-WEB-RELAY | `SOURCE_FACT` |
| V-SRC-22 | 承载端当前是否有运行时组件宿主 | 无；routeTree/router/navigation、`WorkspaceTool` union/内容条件链与i18n均为静态源码，完整二开面为SS-WEB-COMPONENT-HOST | `SOURCE_FACT/SOURCE_SEAM` |
| V-SRC-23 | Mattermost是否证明前端运行时manifest/registry/lifecycle | 有；active manifest、dynamic script、register/init、version reconcile与unload完整（SF-MAT-01/03/04） | `SOURCE_FACT` |
| V-SRC-24 | Mattermost后端plugin能否直接作为Kailo adapter模型 | 否；它启动本机executable并提供宽PluginAPI，且enable为全局config（SF-MAT-02/05） | `SOURCE_FACT` |
| V-SRC-25 | 非真前后端分离组件是否必须拆分才能接入 | 否；Binding/remote adapter固定native scope，Host-native页面独立于native形态；Cells/WeKnora/Wren均有可调用服务接缝（DD-58） | `DERIVED_DESIGN` |
| V-SRC-26 | Mattermost plugin event 是否可直接作多 Tenant runtime 状态 | 否；其 active manifests 与 enable/disable 是全局的。且其 Web 端在事件路径上直接以 payload 携带的 manifest load/remove，全量 reconcile 只在重连时发生——与 DD-61 相反，是反例（SF-MAT-02/06） | `SOURCE_FACT` |
| V-SRC-27 | Buzz runtime component 是否上游已有 | 否；必须在 SS-WEB-COMPONENT-HOST 一次性二开 Component Host，之后才能对兼容 release 运行时注册 | `SOURCE_FACT/SOURCE_SEAM` |
| V-SRC-28 | Buzz 是否有跨 Session Agent memory 协议 | 有；NIP-AE kind:30174，Agent 签名、NIP-44 加密、core/mem head 与 tombstone（SF-BUZ-16） | `SOURCE_FACT` |
| V-SRC-29 | Buzz ACP 是否区分“无 core”与“core 不可读” | 是；只有 confirmed absence 给 nudge，其他错误不注入且 session 不阻塞（SF-BUZ-17） | `SOURCE_FACT` |
| V-SRC-30 | Codex 会话是否只在内存 | 否；非 ephemeral thread 经 ThreadStore 持久且可 resume/分页读取（SF-COD-09） | `SOURCE_FACT` |
| V-SRC-31 | Codex 自动 Memory Pipeline 能否直接当 Kailo 长期记忆权威 | 否；它是默认关闭的 runtime-local DB/file 两阶段合并链，与 NIP-AE 并用会建第二权威（SF-COD-10、DD-65） | `SOURCE_FACT/DERIVED_DESIGN` |
| V-SRC-32 | Buzz `buzz-agent` history/handoff 是否可持久恢复 | 否；它明确为 per-process in-memory，无 external persistence 与 `session/load`（SF-BUZ-21） | `SOURCE_FACT` |
| V-SRC-33 | 关闭 Codex `unified_exec` 是否移除命令执行 | 否；降级为 `ExecCommandHandler::one_shot`，`apply_patch` 亦不受影响；`shell_type` 只能经 `model_catalog_json` 自定义目录设为 disabled 且不影响 `apply_patch`（SF-COD-03/12） | `SOURCE_FACT` |
| V-SRC-34 | CocoIndex `db_path` 是否必填 | 否；缺省静默回落相对路径 `./coco_state`（SF-CIX-04） | `SOURCE_FACT` |
| V-SRC-35 | OpenMeter event 的 `validation_errors` 是否落库 | 否；`om_events` 无该列，每次查询按当时 meter/customer 重算（SF-OMT-07） | `SOURCE_FACT` |
| V-SRC-36 | AgentGateway MCP 认证省略 `audiences` 是否 fail closed | 否；字段已改为可选，省略即传 `None` 关闭校验（SF-AGW-11） | `SOURCE_FACT` |
| V-SRC-37 | WeKnora 文件去重是否含文件名/大小 | 否；仅 `(tenant, kb, type, file_hash, file_type)`，文件名/大小回退在上传路径不可达（SF-WEK-07） | `SOURCE_FACT` |
| V-SRC-38 | AgentGateway admin 未认证面是否仅限 ConfigResource | 否；含覆写配置文件的 `POST /api/config`、`llm.settings`/`mcp.settings` 与 `quitquitquit`（SS-AGW-ADMIN） | `SOURCE_FACT` |
| V-SRC-39 | Temporal application SDK 是否在 `.references` | 是（2026-09-13 起）；`temporal-sdk-go` HEAD `626130f1fd9de50cfd90b884a3fb796504f22dc1`，Kailo Worker 选用的 `v1.48.0` 为其祖先；Server `go.mod` 依赖 `v1.44.0`，两版本独立（SF-TMP-04/08、SF-TSDK-01、DD-69） | `SOURCE_FACT/DERIVED_DESIGN` |
| V-SRC-40 | `thread/start` 的 `environments: []` 是否可直接发送 | 否；该字段为 `#[experimental("thread/start.environments")]`，client 未在 `initialize` 声明 `capabilities.experimentalApi=true` 时 `dispatch_initialized_client_request` 以 `invalid_request` 拒绝；`turn/start.environments` 同理（SF-COD-03） | `SOURCE_FACT` |
| V-SRC-41 | OpenBao transit 能否为 Nostr 密钥签名 | 否；`KeyType_*` 无 secp256k1/Schnorr，全树无 BIP-340 实现；私钥只能以 KV v2 版本托管并由 Core signer 签名（SF-OBA-07，DD-72） | `SOURCE_FACT` |
| V-SRC-42 | OpenBao namespace 是否真实隔离 mount/policy/token | 是；`NamespaceStore` 与 `sys/namespaces` 已实现，mount entry、policy store、token store 均按 namespace 分区，HTTP 层读 `X-Vault-Namespace`（SF-OBA-02） | `SOURCE_FACT` |
| V-SRC-43 | `temporal-sdk-go` HEAD、Kailo Worker `v1.48.0` 与 Server 内部依赖是否同一契约 | 否；`v1.48.0` 是 HEAD 的祖先，HEAD 含后续行为变化；Server 内部依赖为 `v1.44.0`。Kailo 仅以固定 `v1.48.0` 源码约束 Worker，跨 Server 兼容另以真实连通性与 history 重放验证（SF-TMP-04、SF-TSDK-01、DD-69） | `SOURCE_FACT/DERIVED_DESIGN` |
| V-SRC-44 | OpenBao audit 失败时请求是否继续 | 否；`audit_broker` 在没有任何 backend 成功记录时返回错误，请求失败（SF-OBA-06） | `SOURCE_FACT` |
| V-SRC-45 | WeKnora 原生 Memory 能否直接作为 Kailo AgentInstallation 记忆 | 否；它以 WeKnora tenant+caller subject 为唯一 scope，自动抽取并注入 WeKnora chat pipeline，不含 AgentInstallation namespace（SF-WEK-16、DD-65） | `SOURCE_FACT` |

## 4. 最终门禁

| 条件 | 状态 |
|---|---|
| 所有对外 active 能力都有 SOURCE_FACT/SOURCE_SEAM 与唯一权威 | `DESIGN_DEFINED` |
| claims→BFF/logout、Buzz Web Relay/BFF transport（仅 Web）、Component Host、Agent管理/NIP-AE memory、ExtMcp Kailo PEP、WOPI、WeKnora trace、Gateway admin/durable usage、OpenMeter auth适配 | `ADAPTER_REQUIRED: SS-AGW-OIDC、SS-WEB-RELAY、SS-WEB-COMPONENT-HOST、SS-BUZ-AGENT-MODEL、SS-BUZ-ENGRAM、SS-AGW-PEP、SS-CEL-WOPI、SS-OFF-WOPI、SS-WEK-ASYNC-TRACE、SS-WEK-LLM-CTX、SS-AGW-ADMIN、SS-AGW-USAGE、SS-OMT-AUTH` |
| 统一 Secret backend 固定源码 | `UPSTREAM_SUPPORTED`（OpenBao，SF-OBA-01/02/03/04/05/06/07/08/09；Core 集成 `DESIGN_DEFINED`，DD-70/71/72） |
| Temporal application SDK 固定源码 | `UPSTREAM_SUPPORTED`（temporal-sdk-go，SF-TSDK-01/02/03/04/05/06/07/08；Application Worker `DESIGN_DEFINED`，DD-69） |
| 全部 active ResourceType 的 Tenant native 销毁契约 | `BLOCKED: GAP-LCM-01` |
| 一期生产设计门禁 | `ADAPTER_REQUIRED`（SS-AGW-ADMIN/SS-AGW-USAGE/SS-AGW-OIDC/SS-OMT-AUTH/SS-WEK-ASYNC-TRACE）；Tenant 销毁 `BLOCKED: GAP-LCM-01`，Wren native cancel `BLOCKED: GAP-WRN-01` |
