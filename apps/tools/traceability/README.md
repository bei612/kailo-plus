# 追溯记录

每个进入实现的能力一条记录，格式与校验规则以 `apps/06-工程基线规范.md` §1 为准。
文件名 `<capability_id>.yaml`，随代码提交一起演进。

校验由 `tools/check.sh trace` 执行，规则即 §1 的六条硬规则；本目录不重复定义它们。

当前没有记录，因为 Stage 0 不产出任何用户可达能力——这是正确状态，不是遗漏。
第一条记录随 Stage 1 的首个能力加入。
