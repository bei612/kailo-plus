diff --git a/web/.gitignore b/web/.gitignore
index 7d87023..4037ffa 100644
--- a/web/.gitignore
+++ b/web/.gitignore
@@ -13,3 +13,5 @@ test-results
 
 # Kailo contracts 生成物：构建时由 Kailo 放入，不入库（ADR-02/03）
 src/platform/contracts.gen.ts
+# Kailo 共用平台包：构建时由 Kailo 放入，不入库（ADR-09）
+src/kailo-platform/
diff --git a/web/biome.json b/web/biome.json
index 2ef6181..f34bef0 100644
--- a/web/biome.json
+++ b/web/biome.json
@@ -7,7 +7,8 @@
       "!playwright-report",
       "!test-results",
       "!src/app/routeTree.gen.ts",
-      "!src/platform/contracts.gen.ts"
+      "!src/platform/contracts.gen.ts",
+      "!src/kailo-platform"
     ]
   },
   "formatter": {
diff --git a/web/src/platform/bff-client.ts b/web/src/platform/bff-client.ts
index 9ef24f3..229cd65 100644
--- a/web/src/platform/bff-client.ts
+++ b/web/src/platform/bff-client.ts
@@ -4,88 +4,33 @@
 // 不生成 NIP-42/NIP-98/NIP-44，也不提交 raw signed event 或任意 Relay filter。
 // 身份由 AgentGateway 验证后投影给 BFF，浏览器这一侧没有任何可自报的身份字段。
 //
-// 同源请求：页面由同一个网关入口提供，BFF 在其后。因此这里不配置任何主机名——
-// 出现一个可配置的 API 地址，就等于给「连到别处去」留了口子。
-//
-// 请求与回应的形状取自 Kailo contracts 的生成物（ADR-02/03）：`contracts.gen.ts`
-// 由 Kailo 构建时从 contracts/ 生成并放入源树，不入本仓库、不手工编辑。已在
-// contracts 定义的形状这里不另写一份。
+// 与 Desktop 共用的部分——传输接缝、错误解读、会话/成员/审计/设备端点——在 Kailo
+// 的共用包里（@kailo/platform，构建时放入源树，ADR-09），这里只接上 Web 的同源
+// fetch 传输，并补上只有 Web 才有的调用：频道流、经 BFF 代签的发布与媒体、已读与
+// Workspace 偏好、网关退出。
 
 import type {
-  ClientKeyStatus,
-  ClientKeyView,
-  OwnAuditEntry,
-  PlatformSessionView,
   ReadMarkRequest,
   UserStateVersion,
-  WorkspaceMemberView,
   WorkspacePreferenceRequest,
-  WorkspaceView,
-} from "@/platform/contracts.gen";
+} from "@kailo/contracts";
+import { createBffClient } from "@kailo/platform/client";
+import { type BffRequest, unwrap } from "@kailo/platform/transport";
+import { createFetchTransport } from "@kailo/platform/web-fetch";
 
-/**
- * 后端拒绝一次调用。`status` 保留原值；`errorClass`、`reason`、`operationId`
- * 取自统一错误体（Kailo contracts 的 ErrorBody），没有错误体时缺省。
- *
- * `errorClass === "UNKNOWN"` 是外部副作用结果不明：界面不得把它渲染成成功或
- * 失败，只能显示待确认并给出 operationId 作为查证入口。
- */
-export class BffError extends Error {
-  readonly status: number;
-  readonly errorClass?: string;
-  readonly reason?: string;
-  readonly operationId?: string;
-  constructor(
-    status: number,
-    message: string,
-    body?: { class?: string; reason?: string; operationId?: string },
-  ) {
-    super(message);
-    this.name = "BffError";
-    this.status = status;
-    this.errorClass = body?.class;
-    this.reason = body?.reason;
-    this.operationId = body?.operationId;
-  }
-}
+export { BffError } from "@kailo/platform/transport";
 
 /**
- * 网关会话已不在。浏览器只能经一次顶层导航重新登录：fetch 跟随到 IdP 的重定向
- * 只会得到一个看不出原因的 CORS 错误。
- *
- * 两种信号都要认：HTTPS 下网关按 Sec-Fetch-Mode 识别出 fetch，回 401；明文 HTTP
- * 下浏览器不发 Fetch Metadata，网关把请求当成导航，回 302 去 IdP。后者在
- * `redirect: "manual"` 下表现为 opaqueredirect。
+ * 网关会话已不在：浏览器只能经一次顶层导航重新登录，由网关带去 IdP。
  */
-function reauthenticate(): never {
-  window.location.assign(window.location.href);
-  throw new BffError(401, "网关会话已结束，正在重新登录");
-}
+const transport = createFetchTransport({
+  onSessionEnded: () => window.location.assign(window.location.href),
+});
 
-async function call<T>(path: string, init?: RequestInit): Promise<T> {
-  const response = await fetch(path, {
-    ...init,
-    // 会话由网关 cookie 承载，必须随请求发出；浏览器侧不持有任何令牌
-    credentials: "same-origin",
-    redirect: "manual",
-    headers: {
-      ...(init?.body ? { "Content-Type": "application/json" } : {}),
-      ...init?.headers,
-    },
-  });
-  if (response.type === "opaqueredirect" || response.status === 401) reauthenticate();
-  if (!response.ok) {
-    // 错误体只有分类、reason code 与 operationId，不含任何业务内容；取不到
-    // 就当没有，不从状态码猜分类
-    const body = await response.json().catch(() => undefined);
-    throw new BffError(
-      response.status,
-      `${init?.method ?? "GET"} ${path} → ${response.status}`,
-      body,
-    );
-  }
-  if (response.status === 204) return undefined as T;
-  return (await response.json()) as T;
+export const bff = createBffClient(transport);
+
+async function call<T>(request: BffRequest): Promise<T> {
+  return unwrap<T>(request, await transport.send(request));
 }
 
 export type BuzzEvent = {
@@ -108,17 +53,6 @@ export type UserState = {
 /** 一份已上传的媒体（Blossom descriptor）。发消息时原样带回，由 Core 生成 imeta。 */
 export type MediaDescriptor = { url: string; sha256: string; size: number; type: string };
 
-/** 当前会话。撤权后下一次调用即 403——会话不由浏览器持有，不需要它主动丢弃。 */
-export function fetchSession(): Promise<PlatformSessionView> {
-  return call<PlatformSessionView>("/api/v1/session");
-}
-
-/**
- * 注销。
- *
- * 必须先调它再调网关的 logout：网关的 logout 是短路的，请求根本不到后端
- * （SF-AGW-21），先调它就再也没机会告诉 Core，会话会一直留到过期。
- */
 /**
  * 退出。顺序固定：先让 Core 撤销会话并关闭流，再让网关清 cookie。网关的 logout
  * 是短路的，请求到不了后端（SF-AGW-21），反过来做 Core 就再也无从得知。
@@ -130,58 +64,63 @@ export function fetchSession(): Promise<PlatformSessionView> {
  * 普通的顶层导航，由网关带去重新登录。
  */
 export async function signOut(): Promise<void> {
-  await call<{ revoked: boolean }>("/api/v1/logout", { method: "POST" }).catch(() => undefined);
+  await bff.logout().catch(() => undefined);
   await fetch("/logout", { method: "POST", credentials: "same-origin", redirect: "manual" });
   window.location.assign(import.meta.env.BASE_URL);
 }
 
-/** 发一条消息。只给正文——身份、签名、频道归属都由 BFF 决定。 */
 /**
- * 发布一条消息。`idempotencyKey` 标识「这一次发送意图」：结果不明之后重发必须带
- * 同一个键，BFF 据此回答原操作的结论而不是再发一条（Kailo DD-81）。
+ * 发布一条消息。只给正文——身份、签名、频道归属都由 BFF 决定。`idempotencyKey`
+ * 标识「这一次发送意图」：结果不明之后重发必须带同一个键，BFF 据此回答原操作的
+ * 结论而不是再发一条（Kailo DD-81）。
  */
-export function publishMessage(
+export async function publishMessage(
   workspaceId: string,
   content: string,
   attachments: readonly MediaDescriptor[],
   idempotencyKey: string,
 ): Promise<{ eventId: string; operationId: string }> {
-  return call(`/api/v1/workspaces/${workspaceId}/messages`, {
-    method: "POST",
-    headers: { "Idempotency-Key": idempotencyKey },
-    body: JSON.stringify({ content, attachments }),
-  });
+  const path = `/api/v1/workspaces/${workspaceId}/messages`;
+  return unwrap(
+    { method: "POST", path },
+    await transport.exchange(path, {
+      method: "POST",
+      headers: { "Content-Type": "application/json", "Idempotency-Key": idempotencyKey },
+      body: JSON.stringify({ content, attachments }),
+    }),
+  );
 }
 
 export function fetchUserState(): Promise<UserState> {
-  return call<UserState>("/api/v1/user-state");
+  return call<UserState>({ method: "GET", path: "/api/v1/user-state" });
 }
 
 export function setWorkspacePreference(
   workspaceId: string,
   preference: WorkspacePreferenceRequest,
 ): Promise<UserStateVersion> {
-  return call(`/api/v1/user-state/workspaces/${workspaceId}`, {
+  return call({
     method: "PUT",
-    body: JSON.stringify(preference),
+    path: `/api/v1/user-state/workspaces/${workspaceId}`,
+    body: preference,
   });
 }
 
 export function markRead(mark: ReadMarkRequest): Promise<UserStateVersion> {
-  return call("/api/v1/user-state/read", {
-    method: "PUT",
-    body: JSON.stringify(mark),
-  });
+  return call({ method: "PUT", path: "/api/v1/user-state/read", body: mark });
 }
 
-/** 上传一份媒体。Blossom 授权由 Core 代签——浏览器签不出来。 */
 /** 上传一份媒体。签名由 BFF 以本人身份代做，Browser 不接触 Relay。 */
-export function uploadMedia(workspaceId: string, file: File): Promise<MediaDescriptor> {
-  return call<MediaDescriptor>(`/api/v1/workspaces/${workspaceId}/media`, {
-    method: "POST",
-    headers: { "Content-Type": file.type || "application/octet-stream" },
-    body: file,
-  });
+export async function uploadMedia(workspaceId: string, file: File): Promise<MediaDescriptor> {
+  const path = `/api/v1/workspaces/${workspaceId}/media`;
+  return unwrap(
+    { method: "POST", path },
+    await transport.exchange(path, {
+      method: "POST",
+      headers: { "Content-Type": file.type || "application/octet-stream" },
+      body: file,
+    }),
+  );
 }
 
 /** 取一份媒体的可渲染地址。它仍然经 BFF，不指向 Relay。 */
@@ -274,28 +213,3 @@ export function openStream(workspaceId: string, onFrame: (frame: StreamFrame) =>
     source?.close();
   };
 }
-
-/** 本人的设备。登记只能在设备上完成；查看与撤销在任一端都可以。私钥只在那台设备上。 */
-export function fetchClientKeys(): Promise<ClientKeyView[]> {
-  return call<ClientKeyView[]>("/api/v1/identity/client-keys");
-}
-
-/** 撤销一台设备：只移出这一把公钥，Relay 随即拒绝它；其他设备与 Web 不受影响。 */
-export function revokeClientKey(pubkey: string): Promise<ClientKeyStatus> {
-  return call(`/api/v1/identity/client-keys/${encodeURIComponent(pubkey)}`, { method: "DELETE" });
-}
-
-/** 我能进的 Workspace。列表已排除进不去的——列出一个点进去 403 的比不列更糟。 */
-export function fetchWorkspaces(): Promise<WorkspaceView[]> {
-  return call<WorkspaceView[]>("/api/v1/workspaces");
-}
-
-/** 成员按人聚合：`pubkeys` 是此人全部 ACTIVE 的 Buzz 公钥，Web 一把，另加每台原生设备一把（DD-77）。 */
-export function fetchWorkspaceMembers(workspaceId: string): Promise<WorkspaceMemberView[]> {
-  return call<WorkspaceMemberView[]>(`/api/v1/workspaces/${workspaceId}/members`);
-}
-
-/** 基础审计页只看自己的动作；聚合视图需要 audit permission，属于后续阶段。 */
-export function fetchOwnAudit(): Promise<OwnAuditEntry[]> {
-  return call<OwnAuditEntry[]>("/api/v1/audit");
-}
diff --git a/web/src/platform/ui/ChannelPane.tsx b/web/src/platform/ui/ChannelPane.tsx
index 7b9ae7c..c02198a 100644
--- a/web/src/platform/ui/ChannelPane.tsx
+++ b/web/src/platform/ui/ChannelPane.tsx
@@ -3,6 +3,8 @@
 // 全部经 BFF：流、发布、媒体上传与读取、已读写入。这里没有 Relay 地址，也没有
 // signer——签名由 BFF 以本人身份代做。
 
+import { ReasonCode } from "@kailo/contracts";
+import { isOutcomeUnknown } from "@kailo/platform/transport";
 import { useMutation, useQuery, useQueryClient } from "@tanstack/react-query";
 import { Paperclip, X } from "lucide-react";
 import { useCallback, useEffect, useMemo, useRef, useState } from "react";
@@ -278,13 +280,15 @@ function Composer({ workspaceId }: { workspaceId: string }) {
         setPending((current) => current.filter((p) => !attachments.includes(p.descriptor)));
       })
       .catch((e: unknown) => {
-        if (!(e instanceof BffError)) {
-          setProblem(t("platform.sendFailed"));
-        } else if (e.errorClass === "UNKNOWN") {
-          // 结果不明：可能已送达。不说成功也不说失败，草稿保留；若消息随后出现
-          // 在频道里，它就已送达（06 §4、DD-81）
-          setProblem(t("platform.sendUnknown", { operation: e.operationId ?? "" }));
-        } else if (e.reason === "PUBLISH_REJECTED") {
+        if (isOutcomeUnknown(e)) {
+          // 结果不明（没有回应，或 BFF 明说 UNKNOWN）：可能已送达。不说成功也不说
+          // 失败，草稿保留；若消息随后出现在频道里，它就已送达（06 §4、DD-81）
+          setProblem(
+            t("platform.sendUnknown", {
+              operation: e instanceof BffError ? (e.operationId ?? "") : "",
+            }),
+          );
+        } else if (e instanceof BffError && e.reason === ReasonCode.PublishRejected) {
           intent.current = null;
           setProblem(t("platform.sendRejected"));
         } else {
diff --git a/web/src/platform/ui/PlatformApp.tsx b/web/src/platform/ui/PlatformApp.tsx
index 6f44e41..04e0350 100644
--- a/web/src/platform/ui/PlatformApp.tsx
+++ b/web/src/platform/ui/PlatformApp.tsx
@@ -1,32 +1,24 @@
 // Kailo 平台页（SS-WEB-01）。
 //
-// 一期的内置管理页：登录状态、Tenant/Workspace 选择、频道、成员、基础审计。
+// 一期的内置管理页：登录状态、Tenant/Workspace 选择、频道、成员、基础审计、设备。
 // 全部数据经 BFF；这个文件里没有 Relay 地址、没有 signer、没有 Nostr filter。
 //
+// 成员、审计、设备三页是 Kailo 与 Desktop 共用的同一份组件（@kailo/platform，
+// ADR-09），这里只提供 Web 的外壳：Workspace 选择、收藏/静音、频道与退出。
+//
 // 未启用的能力不在这里出现。不渲染一个点进去说「未启用」的入口——
 // 那是把阻断项做成了可见功能。
 
+import type { PlatformSessionView } from "@kailo/contracts";
+import { PlatformProvider } from "@kailo/platform/react/context";
+import { AuditPage, DevicesPage, MembersPane } from "@kailo/platform/react/pages";
 import { useMutation, useQuery, useQueryClient } from "@tanstack/react-query";
 import { BellOff, Star } from "lucide-react";
 import { useCallback, useEffect, useState } from "react";
-import {
-  BffError,
-  fetchSession,
-  revokeClientKey,
-  setWorkspacePreference,
-  signOut,
-} from "@/platform/bff-client";
-import {
-  BuzzIdentityState,
-  type PlatformSessionView,
-  WorkspaceMembershipState,
-} from "@/platform/contracts.gen";
+import { BffError, bff, setWorkspacePreference, signOut } from "@/platform/bff-client";
 import { ChannelPane } from "@/platform/ui/ChannelPane";
 import { platformQueries } from "@/platform/ui/queries";
-import { t } from "@/shared/i18n";
-import { truncatePubkey } from "@/shared/lib/pubkey";
-import { relativeTime } from "@/shared/lib/relative-time";
-import { Badge } from "@/shared/ui/badge";
+import { getLocale, t } from "@/shared/i18n";
 import { Button } from "@/shared/ui/button";
 
 type Tab = "channel" | "members" | "devices" | "audit";
@@ -37,7 +29,8 @@ export function PlatformApp() {
   const [error, setError] = useState<string | null>(null);
 
   useEffect(() => {
-    fetchSession()
+    bff
+      .session()
       .then(setSession)
       .catch((e: unknown) =>
         setError(
@@ -50,7 +43,11 @@ export function PlatformApp() {
 
   if (error) return <Notice text={error} />;
   if (!session) return <Notice text={t("platform.loadingIdentity")} />;
-  return <SignedIn session={session} />;
+  return (
+    <PlatformProvider client={bff} locale={getLocale()}>
+      <SignedIn session={session} />
+    </PlatformProvider>
+  );
 }
 
 function Notice({ text }: { text: string }) {
@@ -103,9 +100,9 @@ function SignedIn({ session }: { session: PlatformSessionView }) {
   // 因此不随 Workspace 是否存在而隐藏。
   const body =
     tab === "audit" ? (
-      <AuditPane />
+      <AuditPage />
     ) : tab === "devices" ? (
-      <DevicesPane />
+      <DevicesPage />
     ) : workspaces.isError ? (
       <Notice text={t("platform.loadFailed")} />
     ) : workspaces.isPending ? (
@@ -115,7 +112,7 @@ function SignedIn({ session }: { session: PlatformSessionView }) {
     ) : tab === "channel" ? (
       <ChannelPane key={active} workspaceId={active} myPrincipalId={session.tenantPrincipalId} />
     ) : (
-      <MembersPane workspaceId={active} />
+      <MembersPane key={active} workspaceId={active} />
     );
 
   return (
@@ -196,142 +193,3 @@ function SignedIn({ session }: { session: PlatformSessionView }) {
     </div>
   );
 }
-
-function MembersPane({ workspaceId }: { workspaceId: string }) {
-  const rows = useQuery(platformQueries.members(workspaceId));
-  if (rows.isPending) return <Notice text={t("platform.loading")} />;
-  if (rows.isError) return <Notice text={t("platform.loadFailed")} />;
-  return (
-    <table className="w-full text-left">
-      <thead className="text-muted-foreground">
-        <tr>
-          <th className="py-1">{t("platform.member")}</th>
-          <th className="py-1">{t("platform.state")}</th>
-          <th className="py-1">{t("platform.protocolIdentity")}</th>
-        </tr>
-      </thead>
-      <tbody>
-        {rows.data.map((m) => (
-          <tr key={m.principalId} className="border-t">
-            <td className="py-1">{m.displayName}</td>
-            <td className="py-1">
-              <Badge
-                variant={m.state === WorkspaceMembershipState.Active ? "secondary" : "outline"}
-              >
-                {m.state}
-              </Badge>
-            </td>
-            <td className="py-1 font-mono text-xs">
-              {m.pubkeys.length > 0 ? m.pubkeys.map(truncatePubkey).join(" · ") : "—"}
-            </td>
-          </tr>
-        ))}
-      </tbody>
-    </table>
-  );
-}
-
-function AuditPane() {
-  const rows = useQuery(platformQueries.audit);
-  if (rows.isPending) return <Notice text={t("platform.loading")} />;
-  if (rows.isError) return <Notice text={t("platform.loadFailed")} />;
-  return (
-    <table className="w-full text-left">
-      <thead className="text-muted-foreground">
-        <tr>
-          <th className="py-1">{t("platform.time")}</th>
-          <th className="py-1">{t("platform.type")}</th>
-          <th className="py-1">{t("platform.action")}</th>
-          <th className="py-1">{t("platform.result")}</th>
-        </tr>
-      </thead>
-      <tbody>
-        {rows.data.map((a, i) => (
-          // 审计行没有对外 id；它们只追加、按时间排序返回，位置即身份
-          // biome-ignore lint/suspicious/noArrayIndexKey: 见上
-          <tr key={i} className="border-t">
-            <td className="py-1 text-xs" title={a.occurredAt}>
-              {relativeTime(Date.parse(a.occurredAt) / 1_000)}
-            </td>
-            <td className="py-1">{a.eventType}</td>
-            <td className="py-1">{a.actionKey}</td>
-            <td className="py-1">
-              <Badge variant={a.decision === "ALLOW" ? "secondary" : "destructive"}>
-                {a.decision}
-              </Badge>{" "}
-              {a.resultCode}
-            </td>
-          </tr>
-        ))}
-      </tbody>
-    </table>
-  );
-}
-
-/**
- * 本人的原生设备（DD-77/79）。
- *
- * 登记只能在设备上完成——私钥在那里生成、从不离开。这里只能查看与撤销：
- * 设备丢了，应当能在浏览器里把它撤掉。撤销只移出这一把公钥，Relay 随即拒绝
- * 它；其他设备与本页所在的 Web 身份不受影响。
- */
-function DevicesPane() {
-  const queryClient = useQueryClient();
-  const keys = useQuery(platformQueries.clientKeys);
-  const revoke = useMutation({
-    mutationFn: revokeClientKey,
-    onSettled: () =>
-      queryClient.invalidateQueries({ queryKey: platformQueries.clientKeys.queryKey }),
-  });
-  if (keys.isPending) return <Notice text={t("platform.loading")} />;
-  if (keys.isError) return <Notice text={t("platform.loadFailed")} />;
-  if (keys.data.length === 0) return <Notice text={t("platform.devices.none")} />;
-  return (
-    <div className="flex flex-col gap-2">
-      <p className="text-xs text-muted-foreground">{t("platform.devices.explain")}</p>
-      {revoke.isError ? (
-        <div className="text-xs text-destructive" role="alert">
-          {t("platform.devices.revokeFailed")}
-        </div>
-      ) : null}
-      <table className="w-full text-left">
-        <thead className="text-muted-foreground">
-          <tr>
-            <th className="py-1">{t("platform.protocolIdentity")}</th>
-            <th className="py-1">{t("platform.state")}</th>
-            <th className="py-1">{t("platform.devices.added")}</th>
-            <th className="py-1" />
-          </tr>
-        </thead>
-        <tbody>
-          {keys.data.map((k) => (
-            <tr key={k.pubkey} className="border-t">
-              <td className="py-1 font-mono text-xs">{truncatePubkey(k.pubkey)}</td>
-              <td className="py-1">
-                <Badge variant={k.state === BuzzIdentityState.Active ? "secondary" : "outline"}>
-                  {k.state}
-                </Badge>
-              </td>
-              <td className="py-1 text-xs" title={k.createdAt}>
-                {relativeTime(Date.parse(k.createdAt) / 1_000)}
-              </td>
-              <td className="py-1 text-right">
-                {k.state === BuzzIdentityState.Revoking ? null : (
-                  <Button
-                    size="sm"
-                    type="button"
-                    variant="outline"
-                    disabled={revoke.isPending}
-                    onClick={() => revoke.mutate(k.pubkey)}
-                  >
-                    {t("platform.devices.revoke")}
-                  </Button>
-                )}
-              </td>
-            </tr>
-          ))}
-        </tbody>
-      </table>
-    </div>
-  );
-}
diff --git a/web/src/platform/ui/queries.ts b/web/src/platform/ui/queries.ts
index f96c80c..6a3cb7d 100644
--- a/web/src/platform/ui/queries.ts
+++ b/web/src/platform/ui/queries.ts
@@ -1,24 +1,16 @@
-// 平台页的读取统一走 react-query。它的 error 态是这里要的关键区分：取不到不是
-// 「空」。把失败渲染成空列表，等于把结果不明说成了确定的「没有」——用户会以为
-// 自己没有 Workspace、没有成员、没有记录。
+// 平台页头部与频道页的读取走 react-query。它的 error 态是这里要的关键区分：取不到
+// 不是「空」。把失败渲染成空列表，等于把结果不明说成了确定的「没有」。
 //
-// 同一份数据只有一个 query key：频道页与成员页共用成员查询，只取一次。
+// 成员、审计、设备页来自 Kailo 共用包（@kailo/platform，ADR-09），它们自带同样
+// 语义的读取，不经这里。
 
-import {
-  fetchClientKeys,
-  fetchOwnAudit,
-  fetchUserState,
-  fetchWorkspaceMembers,
-  fetchWorkspaces,
-} from "@/platform/bff-client";
+import { bff, fetchUserState } from "@/platform/bff-client";
 
 export const platformQueries = {
-  workspaces: { queryKey: ["platform", "workspaces"], queryFn: fetchWorkspaces },
-  audit: { queryKey: ["platform", "audit"], queryFn: fetchOwnAudit },
+  workspaces: { queryKey: ["platform", "workspaces"], queryFn: bff.workspaces },
   userState: { queryKey: ["platform", "user-state"], queryFn: fetchUserState },
-  clientKeys: { queryKey: ["platform", "client-keys"], queryFn: fetchClientKeys },
   members: (workspaceId: string) => ({
     queryKey: ["platform", "members", workspaceId],
-    queryFn: () => fetchWorkspaceMembers(workspaceId),
+    queryFn: () => bff.members(workspaceId),
   }),
 };
diff --git a/web/src/shared/i18n/index.ts b/web/src/shared/i18n/index.ts
index 20a638d..103427e 100644
--- a/web/src/shared/i18n/index.ts
+++ b/web/src/shared/i18n/index.ts
@@ -1,23 +1,16 @@
+import { platformMessages } from "@kailo/platform/i18n";
+
 export type AppLocale = "en" | "zh-CN";
 
+// 平台页（成员、审计、设备）与共用外壳的文案由 Kailo 共用包定义（ADR-09），并入这里
+// 而不在本表另写一份：同一个 key 只有一个来源。
 const messages = {
+  ...platformMessages,
   "message.attachmentImage": { en: "Attachment image", "zh-CN": "附件图片" },
   "message.imageFailed": { en: "Image failed to load", "zh-CN": "图片加载失败" },
   "app.title": { en: "Kailo", "zh-CN": "Kailo" },
-  "platform.title": { en: "Kailo", "zh-CN": "Kailo" },
-  "platform.workspace": { en: "Workspace", "zh-CN": "工作区" },
   "platform.tab.channel": { en: "Channel", "zh-CN": "频道" },
-  "platform.tab.members": { en: "Members", "zh-CN": "成员" },
-  "platform.tab.audit": { en: "Audit", "zh-CN": "审计" },
-  "platform.signOut": { en: "Sign out", "zh-CN": "退出" },
   "platform.loadingIdentity": { en: "Resolving identity…", "zh-CN": "正在解析身份…" },
-  "platform.sessionUnavailable": { en: "Session unavailable", "zh-CN": "会话不可用" },
-  "platform.loading": { en: "Loading…", "zh-CN": "载入中…" },
-  "platform.loadingWorkspaces": { en: "Loading workspaces…", "zh-CN": "正在载入工作区…" },
-  "platform.noWorkspace": {
-    en: "You have no workspace you can enter in this tenant",
-    "zh-CN": "你在该 Tenant 下还没有可进入的工作区",
-  },
   "platform.stream.connecting": { en: "Connecting", "zh-CN": "连接中" },
   "platform.stream.synced": { en: "Synced", "zh-CN": "已同步" },
   "platform.stream.revoked": {
@@ -32,10 +25,6 @@ const messages = {
     en: "Disconnected — reload to reconnect",
     "zh-CN": "连接已断开，刷新页面重连",
   },
-  "platform.loadFailed": {
-    en: "Couldn't load this — the result is unknown. Reload to try again.",
-    "zh-CN": "未能载入，结果不明。请刷新重试。",
-  },
   "platform.attach": { en: "Attach image or video", "zh-CN": "添加图片或视频" },
   "platform.attachments": { en: "Attachments", "zh-CN": "附件" },
   "platform.removeAttachment": { en: "Remove", "zh-CN": "移除" },
@@ -51,21 +40,6 @@ const messages = {
     en: "Mute this workspace (no unread count in the tab title)",
     "zh-CN": "静音此工作区（标签页标题不显示未读数）",
   },
-  "platform.tab.devices": { en: "Devices", "zh-CN": "设备" },
-  "platform.devices.none": {
-    en: "No devices yet. Sign in to Kailo Desktop or Mobile to add one.",
-    "zh-CN": "还没有设备。在 Kailo Desktop 或 Mobile 上登录即可添加。",
-  },
-  "platform.devices.explain": {
-    en: "Each device holds its own key. Revoking one stops only that device.",
-    "zh-CN": "每台设备各持自己的密钥。撤销只停用那一台设备。",
-  },
-  "platform.devices.added": { en: "Added", "zh-CN": "添加于" },
-  "platform.devices.revoke": { en: "Revoke", "zh-CN": "撤销" },
-  "platform.devices.revokeFailed": {
-    en: "Couldn't revoke — the result is unknown. Reload to check.",
-    "zh-CN": "撤销未完成，结果不明。请刷新后确认。",
-  },
   "platform.sendFailed": { en: "Send failed", "zh-CN": "发送失败" },
   "platform.sendUnknown": {
     en: "Delivery not confirmed yet. If the message shows up in the channel, it was delivered. Reference: {operation}",
@@ -74,13 +48,6 @@ const messages = {
   "platform.sendRejected": { en: "The message was rejected", "zh-CN": "消息被拒绝" },
   "platform.message": { en: "Message", "zh-CN": "消息" },
   "platform.send": { en: "Send", "zh-CN": "发送" },
-  "platform.member": { en: "Member", "zh-CN": "成员" },
-  "platform.state": { en: "State", "zh-CN": "状态" },
-  "platform.protocolIdentity": { en: "Protocol identity", "zh-CN": "协议身份" },
-  "platform.time": { en: "Time", "zh-CN": "时间" },
-  "platform.type": { en: "Type", "zh-CN": "类型" },
-  "platform.action": { en: "Action", "zh-CN": "动作" },
-  "platform.result": { en: "Result", "zh-CN": "结果" },
   "error.attachmentImagePrepare": {
     en: "We couldn't prepare this image for upload.",
     "zh-CN": "无法处理此图片以上传。",
diff --git a/web/src/shared/styles/globals.css b/web/src/shared/styles/globals.css
index 24e7998..614ccfe 100644
--- a/web/src/shared/styles/globals.css
+++ b/web/src/shared/styles/globals.css
@@ -1,6 +1,8 @@
 @import "tailwindcss";
 @import "tw-animate-css";
 @config "../../../tailwind.config.js";
+/* Kailo 共用平台包放在被忽略的目录里，Tailwind 不自动扫描，显式登记（ADR-09） */
+@source "../../kailo-platform";
 
 @layer base {
   :root {
diff --git a/web/tsconfig.json b/web/tsconfig.json
index 456359a..d97be62 100644
--- a/web/tsconfig.json
+++ b/web/tsconfig.json
@@ -6,7 +6,9 @@
     "module": "ESNext",
     "skipLibCheck": true,
     "paths": {
-      "@/*": ["./src/*"]
+      "@/*": ["./src/*"],
+      "@kailo/contracts": ["./src/platform/contracts.gen.ts"],
+      "@kailo/platform/*": ["./src/kailo-platform/*"]
     },
 
     /* Bundler mode */
diff --git a/web/vite.config.ts b/web/vite.config.ts
index 1fbb430..5728277 100644
--- a/web/vite.config.ts
+++ b/web/vite.config.ts
@@ -18,7 +18,10 @@ export default defineConfig({
     react(),
   ],
   resolve: {
+    // Kailo 放入源树的共用包与 contracts 生成物（ADR-09），以包名引用
     alias: {
+      "@kailo/contracts": "/src/platform/contracts.gen.ts",
+      "@kailo/platform": "/src/kailo-platform",
       "@": "/src",
     },
   },
