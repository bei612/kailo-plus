[package]
name = "contracts"
version.workspace = true
edition.workspace = true
publish.workspace = true

[dev-dependencies]
serde_json = "1"

[dependencies]
serde = { version = "1", features = ["derive"] }
serde_json = "1"
