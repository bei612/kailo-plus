{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "domain/task_state.schema.json",
  "title": "TaskStateReport",
  "description": "Workflow 经 ProjectTaskState Activity 写回 Core 的一次状态跃迁（.design/06 §3.1）。Core 按 workflowId 单调 upsert，eventId 不大于已有值的报告按幂等成功忽略。",
  "type": "object",
  "additionalProperties": false,
  "required": ["workflowId", "runId", "eventId", "status"],
  "properties": {
    "workflowId": { "type": "string", "description": "固定格式的业务 workflow ID" },
    "runId": { "type": "string" },
    "eventId": { "type": "integer", "description": "报告时的 history 长度；同一 workflow 内单调，重放时取到同一值" },
    "status": { "$ref": "../enums/task_status.schema.json" },
    "waitingReason": { "type": "string" },
    "progress": { "type": "string" }
  }
}
