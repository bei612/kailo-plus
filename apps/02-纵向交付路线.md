# 纵向交付路线

## 1. 交付方法

每个 Stage 是可运行的纵向切片，不是按技术层分工的瀑布。切片必须同时包含：

1. 本 Stage 所声明各端的用户入口（见 §1.1）；
2. BFF typed query 或 semantic command；
3. Core 领域状态与事务；
4. scope guard 与 fresh authorization；
5. 所需 Approval/Workflow；
6. Capacity/Quota/Usage 的明确策略；
7. Audit、operation correlation 与用户可见状态；
8. native system 的真实终态和失败语义；
9. 主题、i18n、可访问性与权限裁剪；
10. 自动化验证、回滚与设计追溯。

前一 Stage 的退出门禁是后一 Stage 的依赖。允许并行实现内部模块，不允许绕过入口 Stage 将未治理能力直接暴露给用户或 Agent。

## 1.1 端交付规则

一期交付 Buzz Web、Buzz Desktop、Buzz Mobile 三端（`REQ-01`、`DD-74`）。本节固定每个 Stage 的端归属，消除「三端同时交付」在排期上的歧义：它指一期结束时三端都可用，不指每个 Stage 的每条能力在三端同时落地。

| 端 | 参与范围 | 依据 |
|---|---|---|
| Buzz Web | 全部 Stage 的完整能力面 | 基线最薄，是设计验证的第一现场 |
| Buzz Desktop | 与 Web 同步的完整能力面；主体是 `block/buzz` 的 `desktop/`（Tauri），与 Web 的 buzz-web 不是同一代码库（`DD-74`），协作面本机持钥直连 Relay、管理面经原生 listener 进 BFF（`DD-75`、`DD-78`）；Component Host 与 Web 共用同一份契约（`REQ-11`） | `DD-74`、`DD-75`、`DD-78`、`REQ-11` |
| Buzz Mobile | Stage 1–2 交付协作数据平面与登录、成员视图；Stage 3 起的管理面能力以受权只读视图交付；组件宿主与 Office 编辑不交付 | `REQ-21`、`DD-74` |

硬规则：

- 每个 Stage 的退出门禁必须在该 Stage 声明的**每个**端上各自成立，不允许以 Web 通过代替其余端。
- Buzz Mobile 上未交付的能力按 `V-SCN-65` 不渲染入口；经深链接进入时返回稳定 reason code 并指向 Web/Desktop，不得以内嵌 WebView 绕过。
- 三端对同一 Governed Action 的准入、审批、额度与审计结论必须相同；端差异只体现为入口有无，不体现为治理结论不同（`REQ-21`）。
- 原生端的协作数据平面走本机签名直连 Relay，管理平面经 BFF（`DD-75`）。任一 Stage 的退出门禁在原生端验收时必须同时覆盖这两条路径。

## 2. Stage 0：工程与合同基线

### 交付

- 建立 `01-工程结构与模块边界.md` 的最小目录与发布单元；
- 固定 contract schema、生成策略、兼容规则与追溯清单；
- 完成 Core 持久化、部署、CI、artifact/provenance 等 ADR；
- 建立 migration、outbox、idempotency、error taxonomy 和 feature exposure 基础库；
- 建立上游 baseline/patch manifest，记录固定 commit 与 `SS-*`；
- 建立一条从提交到可验证 artifact 的供应链。

### 退出门禁

- 所有语言绑定通过 schema round-trip 与 compatibility 检查；
- 空迁移可前进、回滚或安全停止；
- 空 Workflow history 可 replay；
- artifact 可追溯到源码 commit、依赖锁和 digest；
- 设计追溯检查能拒绝无来源能力和 `BLOCKED` exposure。

## 3. Stage 1：身份、Tenant/Workspace 与三端协作

### 目标链

```text
OIDC login
→ AgentGateway verified issuer/subject
→ Core PlatformSession/HumanIdentity
→ Tenant/Workspace membership
→ SpiceDB relationship + Buzz Community/Channel roster projection
→ Buzz Web 经 BFF 读写频道；Desktop/Mobile 本机签名直连 Relay
→ Audit/operation timeline
```

### 实施内容

身份与协作面：

- 闭合 `SS-AGW-OIDC`：固定内部身份 header、cookie 前缀/大小、logout 协同和 CORS fail-closed；
- 闭合 `SS-WEB-RELAY`：替换 Browser 直连 Relay 的 query/subscribe/publish/media/user-state 路径；
- 闭合 `SS-BUZ-SERVER-CLIENT`：把上游单身份 harness 扩展为 BFF 的多身份连接与查询面，按 `(principal, active session)` 复用 NIP-42 会话，写入与历史查询走无状态 NIP-98，并固定 NIP-11 上界与限流预算；
- 闭合 `SS-WEB-01`：在 buzz-web 构建期静态 route/navigation 中加入登录、Tenant/Workspace 选择、频道、成员与基础审计页；Buzz Desktop 在其 Tauri 主体中、Buzz Mobile 在其 Dart 主体中分别提供同一组页面（Mobile 为只读或协作子集），两端经原生 listener 取数（`DD-78`）；
- 原生设备公钥登记与撤销（`DD-77`、`DD-79`）：设备以绑定会话的持钥证明登记 CLIENT pubkey，`BUZZ_IDENTITY_PROJECTION` 投入与移出 roster；成员投影与撤权覆盖此人全部 pubkey；
- 托管身份的轮换（`.design/09` 的 key revoke/rotate 与 RelayOperatorIdentity rotate 两行）：Web 托管 HUMAN 身份经两条 `BUZZ_IDENTITY_PROJECTION` 先撤后建，operator 身份以 allow-list 重叠窗口在引导路径上切换；CONTROL 受 `GAP-BUZ-01` 阻断；
- 闭合 `SS-BUZ-OPERATOR`：Community 创建凭据链与 NIP-98 目标约束；
- 实现 PlatformSession、Tenant/Workspace、membership、BuzzIdentityBinding 与 RelayOperatorIdentity；
- 实现完整 snapshot + generation 的 BFF stream 恢复。

本 Stage 自带的承重底座（不下放到 Stage 2，否则本 Stage 无法收口）：

- OpenBao 最小面：namespace 划分、KV v2 写入与版本、`SecretRef` 状态机、Core service token 与引导凭据、audit device 前置校验。Human 独立 Nostr key 的托管与 Core 代签直接建立在它之上；
- Temporal 最小面：client、Worker 骨架、`ComponentTaskWorkflow` 的 `TENANT_LIFECYCLE`/`WORKSPACE_LIFECYCLE`/`MEMBERSHIP_PROJECTION`/`MEMBERSHIP_REVOCATION` 四个 kind、`WorkflowRef` 与固定 workflow ID、Start 的 reuse/conflict policy、`ProjectTaskState` 投影 Activity、Activity 超时与重试纪律。

底座只覆盖本 Stage 生命周期链所需的范围。ActionDefinition/Execution 全模型、Approval 全链、任务工作台 UI、Capacity/Quota 仍属 Stage 2。

### 明确限制

- 不启用 Agent、Component remote module、Office、知识库或问数；
- Tenant delete 与 Workspace delete 不注册；
- Tenant CONTROL 身份的 key revoke/rotate 不注册（`GAP-BUZ-01`），入口对 CONTROL 回 `409`；
- Browser 不获得 Relay URL、Nostr 私钥或任意 Relay filter；
- 收藏、静音、已读使用 Core `CollaborationUserState`，不复用 Browser NIP-44 权威。

### 验收映射

`V-SCN-01`、`02`、`03`、`13`、`28`、`29`、`32`、`34`、`35`、`36`、`43`、`63`、`64`。

### 退出门禁

- `SS-AGW-OIDC`、`SS-WEB-RELAY`、`SS-BUZ-SERVER-CLIENT`、`SS-WEB-01`、`SS-BUZ-OPERATOR` 各有接缝成立的可复核证据；
- 登录、logout、断线恢复端到端通过；
- 成员建立与撤权经 `MEMBERSHIP_PROJECTION`/`MEMBERSHIP_REVOCATION` 完成 SpiceDB 与 Channel roster 对账，Core `REVOKING` 在对账完成前即拒绝新动作；
- Workflow Start 响应不明时按固定 workflow ID 收敛，不产生第二个业务 workflow ID；
- 外来身份 header、缺失 header、跨 scope stream 和 Browser 直连全部被拒绝；
- 每次消息动作可关联 actor、scope、operation、Buzz event 与 AuditEvent。

## 4. Stage 2：统一治理内核、Temporal 与任务工作台

### 目标链

```text
Client Surface semantic command
→ Action preview/admission
→ SpiceDB fresh Check
→ ApprovalWorkflow 或 ComponentTaskWorkflow
→ ExternalExecution/observe/reconcile
→ TaskProjection + Audit
→ 三端 Tasks/Approvals（Mobile 只读）
```

### 实施内容

- 实现 ActionDefinition/Execution、Operation、ApprovalPolicy/Projection、WorkflowRef、TaskProjection、ExternalExecution；
- 在 Stage 1 底座上补齐 `ApprovalWorkflow`、`AgentTaskWorkflow` 与 `.design/06` 其余 `ComponentTaskWorkflow` kind；
- 固定 Update ID、Validator 的确定性边界、retry、heartbeat、continue-as-new 与 UNKNOWN 语义；Approval 决定、撤回及参数确认走 Update，运行中 Workflow 取消走 `RequestCancelWorkflowExecution`，RPC 前冻结首次 run 与 UNKNOWN 意图；RPC 成功仍会是 no-op，只有同一链 history 中匹配控制 ID 的事件才证明本次请求已记录，仍须等待 Worker 收尾与 Temporal 终态（`DD-69/84`、`SF-TMP-01/11`）；
- 把 `ProjectTaskState` 扩展到全部 kind，并建立按 Tenant 的 Temporal 对账作业；
- 实现 owner、fresh approval admission、职责分离和 consumed/invalidated；
- 把 Stage 1 的 OpenBao 最小面扩展为完整 SecretRef 生命周期：audience、version、lease、轮换顺序与各消费端投递；
- 实现 CapacityLease 与 Activity heartbeat/terminal evidence；
- 实现 Tasks、Approvals、详情、取消、重试和 evidence 导航；Web 与 Desktop 完整，Mobile 为受权只读视图。

### 验收映射

`V-SCN-05`–`08`、`14`、`15`、`31`–`38`、`41`–`44`、`67`、`68`。

### 退出门禁

- 录制 history 在同版本和升级版本 Worker 上 replay 通过；
- 审批不能由 Core 直接改状态，批准后 dispatch 前会重新准入；
- Workflow Start 响应不明不会生成第二个业务 workflow ID；
- Activity heartbeat 超时不会直接复用 Capacity slot；
- 任务投影落后显示 `PROJECTION_DELAYED`，不伪造 terminal；
- `GAP-LCM-01` 仍使 Tenant delete Action 不可见。

## 5. Stage 3：Component Catalog、运行时 Host 与 Adapter Protocol

### 目标链

```text
Platform admin 登记 immutable release
→ artifact/schema/digest/治理合同验证
→ ApprovalWorkflow
→ exact binding generation 对账
→ Host snapshot 或 Adapter dispatch
→ disable/revoke/rollback
```

### 实施内容

- 实现 ComponentDefinition/Release/Package、PlatformProviderBinding、ApplicationBinding 与 RuntimeProjection；
- 实现 Platform Port Router 和 Application Adapter Dispatcher；
- 实现 Adapter Protocol handshake/validate/resolve/execute/observe/cancel/reconcile/usage/error mapping；
- 闭合 `SS-WEB-COMPONENT-HOST` 与 `SS-WEB-PRESENTATION`；
- 实现内容寻址 artifact route、静态 import 边界扫描、pending load、generation、dispose 和错误归因；
- 实现 release 登记、批准、撤销、binding 建立/升级/回滚/停用 Workflow；
- 首批组件 UI 继续使用 `HOST_NATIVE`，Remote module 先以无业务副作用的样例 release 验证宿主合同。
- 交付 `REQ-22` 的六件第三方接入面（`DD-76`）：由 `contracts/` 派生的 TypeScript Component SDK、manifest schema 与本地校验器、脚手架、可脱离真实平台运行的本地开发环境、语言无关的 Adapter Protocol 一致性测试套件、端到端示例组件。

### 验收映射

`V-SCN-18`、`33`、`47`–`57`、`65`、`66`。

### 退出门禁

- 未批准、digest 不符、API 不兼容、越界 import、缺 i18n key 的 artifact 均拒绝登记或加载；
- 新 generation 校验失败不切流，旧 invocation 固定旧 generation；
- binding 停用先 fail closed，再撤 route/tool/schedule/projection 并保留历史；
- manifest 不能创建未知 platform port 或加载未编译 driver；
- 兼容 release 能在不重发 Core 与两个承载端的条件下注册，超出既有 slot/API/port/protocol 的 release 被拒绝。

## 6. Stage 4：Quota、Usage、Billing 与业务可观测性

### 目标链

```text
Action admission
→ CapacityLease/QuotaReservation
→ native usage evidence
→ durable outbox
→ OpenMeter CloudEvent
→ committed usage/entitlement/billing projection
→ operation timeline
```

### 实施内容

- 闭合 `SS-OMT-AUTH`，只有 Core 能访问 OpenMeter；
- 实现 Tenant→Customer、scope→subject 和固定 CloudEvent 字段映射；
- 实现 strict reservation 的封闭 producer 集、可证上界和串行化；
- 闭合 `SS-AGW-USAGE`，让 LLM usage 具备持久确认、稳定 log ID 与可重放 cursor；
- 实现 Audit/Usage outbox、dedupe、commit 对账、账单状态与缺口状态；
- 实现按权限联合查询 operation→action/workflow/external execution/audit/usage/native evidence；
- 建立 redaction 与 evidence reference 的当前权限解析。
- 一名未参与平台开发的工程师仅凭接入面完成一个组件、通过一致性套件并成功登记 binding；过程中不读 `.design` 全文、不接触平台源码。

### 验收映射

`V-SCN-14`–`17`、`27`、`41`、`43`、`44`、`62`。

### 退出门禁

- OpenMeter 202 只显示 accepted，`stored_at` 后才显示 committed；
- 相同 namespace/source/id/body 的重试只形成一次 committed usage；
- durable usage 缺失或 correlation 缺失显示 `BILLING_UNAVAILABLE`，不显示零费用；
- strict quota 只用于全部 producer 受控且上界可证的 meter；
- reverse-proxy 不向 upstream 泄漏 `Proxy-Authorization`；
- 用户只能读取当前权限允许的 audit/evidence。

## 7. Stage 5：Agent 管理、Codex runtime、工具与记忆

### 目标链

```text
Agent Definition → immutable Version → Workspace Installation
→ RuntimeProjection/Gateway projection/Buzz roster
→ Relay trigger → AgentTaskWorkflow → Codex turn
→ MCP elicitation/Temporal Approval → AgentGateway PEP → Tool
→ Buzz reply + usage/audit + durable thread/NIP-AE memory
```

### 实施内容

- 闭合 `SS-BUZ-AGENT-MODEL`、`SS-COD-RUNTIME`、`SS-COD-APP`、`SS-AGW-PEP`、`SS-AGW-ADMIN`；
- 实现 AgentDefinition/Version/Installation、SkillVersion、ToolBinding、Delegation 与 RuntimeProjection；
- 实现 per-Installation Codex Runtime Supervisor、隔离 CODEX_HOME/sqlite_home、health/restart/drain；
- 实现无 local environment 的承重封堵和 host RPC 禁用；
- 实现 Gateway route/policy 的 Core-only 写入、read-back hash 与撤权 reload/interrupt；
- 实现 tool 逐方法 RBAC、ActionToken、jti/iat/parameter hash/delegation/exposure/fresh SpiceDB PEP；
- 闭合 `SS-COD-TRACE`，把每次模型调用与 operation 关联；
- 闭合 `SS-BUZ-ENGRAM`，实现 core/cold NIP-AE 读写、Approval、head 对账和时钟异常；
- Web 提供 Agent、Version、Installation、Tool/Skill、权限、运行状态和 Memory 页面。

### 验收映射

`V-SCN-04`、`06`、`07`、`11`、`12`、`16`、`17`、`23`、`39`、`40`、`46`、`58`–`62`。

### 退出门禁

- Agent 无法使用未同时满足 permission、Delegation、ToolBinding、Exposure 与 binding scope 的工具；
- 高风险工具在 backend call 前暂停并进入 Temporal Approval；
- 撤权会 interrupt 在途 turn、重建 MCP client，并阻止后续副作用；该收敛方式是 `GAP-AGW-01` 的既定绕行，产品不得声称已建连接上的 bearer 被原地更换；
- shell/process/apply_patch/local environment 路径均不可达；
- Codex 重启以原 `runtime_thread_id` resume，不由 Relay 重放伪造 rollout；
- NIP-AE `ABSENT` 与 `UNREADABLE` 可区分，core 冲突与 head 超前 fail closed；
- LLM usage、cost、trace、operation 和 Agent invocation 可完整关联。

## 8. Stage 6：Cells 云盘与 ONLYOFFICE

### 目标链

```text
承载端 Drive
→ Core Resource/owner/permission
→ Cells adapter
→ WOPI launch session
→ DocumentServer Check/Get/Put
→ Cells NodeVersions evidence
→ DocumentSession saved/conflict/unknown
```

### 实施内容

- 实现 Cells Resource、FileReference、owner、preview/download/upload 与版本视图；
- 闭合 `SS-CEL-WOPI`、`SS-OFF-WOPI`；
- 实现平台 Human/session/permission 到 Cells PAT/WOPI 的安全投影；
- 实现 PutFile 前置 PEP、timestamp、partial write、NodeVersions 对账与 `DOCUMENT_RECONCILE`；
- 固定 DocumentServer ipfilter、JWT direct request 与 private IP 相关部署不变式；
- 实现主题/locale WOPI launch 映射和聊天附件预览/编辑入口。

### 验收映射

`V-SCN-24`–`26`、`45`，以及 `.design/18` 的 WOPI 交互矩阵。

### 退出门禁

- read-only session 无法因 Cells 原生默认行为获得写权限；
- 撤权后新 launch 与既有 session 保存都 fail closed；
- 只有 PutFile evidence 与新 NodeVersions head 同时成立才显示 SAVED；
- 409、partial write、500 with bytes 和响应不明进入确定冲突或 UNKNOWN 对账；
- DocumentServer 只能请求绑定 Tenant 的 Cells origin。

## 9. Stage 7：WeKnora 知识库与 CocoIndex 增量物化

### 目标链

```text
Cells complete snapshot
→ stable node/revision/hash
→ Temporal Schedule/ComponentTaskWorkflow
→ CocoIndex keyed diff
→ WeKnora create-new/readiness/delete-old
→ citations/graph consume + usage/audit
```

### 实施内容

- 实现 WeKnora KB/knowledge/file/task 的 Resource、owner、permission 与 native scope；
- 闭合 `SS-CEL-MATERIALIZATION`、`SS-WEK-MATERIALIZATION`、`SS-WEK-ASYNC-TRACE`、`SS-WEK-LLM-CTX`；
- 实现两把最小权限 API key 或移除无法安全提供的 export Action；
- 实现 CocoIndex runner 的绝对 `db_path`、稳定 app name、state backup/restore 与 component 级失败隔离；
- 实现完整 snapshot 门禁、hash 引用集合、create-new→ready→delete-old 和 partial snapshot 禁删；
- WeKnora 图谱作为同一 KB 的可选索引，不引入 EdgeQuake 第二图谱；
- Agent consume 只返回回答、citation 与受控 derived Asset。

### 验收映射

`V-SCN-05`、`19`–`22`，以及 `.design/13` 的增量失败语义。

### 退出门禁

- 同 node 内容变更、同内容多 node、删除、runner 迁移、部分 snapshot、WeKnora 失败均可恢复；
- state 不可达时不会换空目录继续；
- 新知识 ready 前旧知识保持可用；
- export 不会通过一个拥有隐含写权限的通用 credential 暴露；
- 图谱启用条件、检索范围和 Tool 权限与源码合同一致。

## 10. Stage 8：Wren 智能问数与一期收口

### 目标链

```text
SemanticModelVersion publish
→ per-model runtime + MCP target
→ Client Surface/Agent semantic query
→ AgentGateway → ExtMcp PEP → Wren
→ bounded result/Asset + usage/audit
```

### 实施内容

- 实现 SemanticModel/Version、发布/下线、owner、permission 与 `MODEL_PUBLISH`；
- 实现每 model version 固定 runtime、profile SecretRef、只读数据库 role；
- 固定 `no_connect=false`、`allow_write=false`，通过 ExtMcp 做逐方法 RBAC；
- query 类映射 `execute`，模型定义类映射 `read`，`store_query` 保持 `EXCLUDED`；
- 实现 query timeout、平台停止等待、迟到结果丢弃与结果行数/字段暴露限制；
- 完成全平台安全、恢复、性能、可访问性、主题/i18n 和运维演练。

### 验收映射

`V-SCN-09`、`10`、`45`、`46`、`49`，以及 `.design/08` 的 Wren 权限映射。

### 退出门禁

- 数据库原生只读 role 是最终写保护；Wren SQL policy 不被当成安全边界；
- `GAP-WRN-01` 保持 native cancel 不可用，产品只显示平台停止等待；
- 不同 model version 的 runtime、credential、MCP target 和 usage 不串用；
- 一期生产总门禁与 `03-验证发布与验收门禁.md` 全部通过。

## 11. 不进入路线的能力

- Tenant 销毁：`BLOCKED: GAP-LCM-01`；
- Wren native query cancel：`BLOCKED: GAP-WRN-01`；
- Tenant CONTROL 身份的轮换与撤销：`BLOCKED: GAP-BUZ-01`。泄漏只以 KV `delete` 遏制，该 Tenant 的投影随之 fail closed；
- 长存 MCP bearer 的原地轮换：`GAP-AGW-01`。撤权只以结束旧 turn 与重建 client 收敛，不承诺已建连接换 token；
- 跨 Tenant 资源转移（`V-SCN-30`）：首批全部 ResourceType 的 `transfer_formats=[]`，`RESOURCE_EXPORT`/`RESOURCE_IMPORT` Action 在一期不注册。领域合同已在 `.design` 冻结，但没有可交付的用户入口；
- Cells 自有 editor provider 接缝（`SS-CEL-EDITOR`）：设计已判定它只能返回 `PreSignedURL`，不足以表达承载端所需的结构化 editor config，不作为一期主接缝；
- 跨 Tenant 直接共享；
- Desktop、Mobile 与本地 computer use；
- 第二套聊天、图谱、Workflow、Agent Registry、Memory 或计费权威；
- Mattermost、AgentTeams、AgentRegistry、CopilotKit、EdgeQuake、MinerU、RAGFlow、temporal-ui 的运行时集成；
- Tenant 上传后端 executable、动态库或未知 embedded driver。
