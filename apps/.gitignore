# Local secrets and overrides
.env
.env.*
!.env.example
*.key
*.pem

# Dependency and build output
node_modules/
target/
build/
dist/
coverage/
*.log

# Local tool state
.cache/
.tmp/
.idea/
.vscode/
.DS_Store

# Trellis 工作流（本机工具状态，不进版本库）
# 由 `trellis init` 生成；重建方式见 README「工具链」
# 例外 .trellis/spec/：从代码提炼的编码约定知识库，是工程资产，随仓库分发
.trellis/*
!.trellis/spec/
.claude/
.codex/
.agents/

# Go
/worker/bin/

# Dart / Flutter
/mobile/.dart_tool/
/mobile/pubspec.lock

# 本地开发 secret（由 deploy/local/bootstrap.sh 生成）
/deploy/local/secrets/

# 发布产物（由 tools/release.sh 生成，可追溯性来自 provenance）
/dist/

# OpenBao raft 数据（本地拓扑，不进版本库）
/deploy/local/data/

# 本地 registry 数据
/deploy/local/data/registry/

# Python 字节码缓存（tools/upstream_manifest.py 被 check.sh 导入时生成）
__pycache__/
